# WhatJobs — India publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# IN account.
#
# The publisher id is NOT here: it lives in the "in:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-in" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs IN — Java developers
  board: "java developer" # ~109, collapses at page 4
- company: WhatJobs IN — Full stack developers
  board: "full stack developer" # ~81, collapses at page 3
- company: WhatJobs IN — Data engineers
  board: "data engineer" # ~75, collapses at page 3
- company: WhatJobs IN — DevOps engineers
  board: "devops engineer" # ~20, collapses at page 1
- company: WhatJobs IN — React developers
  board: "react developer" # ~18, collapses at page 1
- company: WhatJobs IN — Python developers
  board: "python developer" # ~17, collapses at page 1
- company: WhatJobs IN — Backend developers
  board: "backend developer" # ~15, collapses at page 1
- company: WhatJobs IN — Frontend developers
  board: "frontend developer" # ~14, collapses at page 1
