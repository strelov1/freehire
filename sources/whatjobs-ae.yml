# WhatJobs — United Arab Emirates publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# AE account.
#
# The publisher id is NOT here: it lives in the "ae:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-ae" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs AE — Backend developers
  board: "backend developer" # ≥395 corroborated through page 8 tested, still going
- company: WhatJobs AE — Data engineers
  board: "data engineer" # ~290, feed runs dry through page 8 tested
- company: WhatJobs AE — Python developers
  board: "python developer" # ~161, collapses at page 6
- company: WhatJobs AE — DevOps engineers
  board: "devops engineer" # ~143, collapses at page 4
- company: WhatJobs AE — Frontend developers
  board: "frontend developer" # ~131, collapses at page 4
- company: WhatJobs AE — Full stack developers
  board: "full stack developer" # ~129, collapses at page 4
- company: WhatJobs AE — Java developers
  board: "java developer" # ~55, collapses at page 2
