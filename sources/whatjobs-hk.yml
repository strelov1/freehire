# WhatJobs — Hong Kong publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# HK account.
#
# The publisher id is NOT here: it lives in the "hk:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-hk" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs HK — Java developers
  board: "java developer" # ~263, collapses at page 8
- company: WhatJobs HK — Data engineers
  board: "data engineer" # ≥293 through page 8 tested, still going
- company: WhatJobs HK — Backend developers
  board: "backend developer" # ~167, feed runs dry at page 5
- company: WhatJobs HK — Python developers
  board: "python developer" # ~83, feed runs dry at page 3
- company: WhatJobs HK — Full stack developers
  board: "full stack developer" # ~87, collapses at page 3
- company: WhatJobs HK — Frontend developers
  board: "frontend developer" # ~59, collapses at page 2
- company: WhatJobs HK — DevOps engineers
  board: "devops engineer" # ~57, collapses at page 2
