# WhatJobs — South Africa publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# ZA account.
#
# The publisher id is NOT here: it lives in the "za:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-za" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs ZA — Backend developers
  board: "backend developer" # ~299, feed runs dry at page 8
- company: WhatJobs ZA — Full stack developers
  board: "full stack developer" # ≥342 through page 8 tested, still going
- company: WhatJobs ZA — Data engineers
  board: "data engineer" # ~303 through page 8 tested, still going
- company: WhatJobs ZA — DevOps engineers
  board: "devops engineer" # ~144, collapses at page 5
- company: WhatJobs ZA — Python developers
  board: "python developer" # ~60, collapses at page 2
- company: WhatJobs ZA — Frontend developers
  board: "frontend developer" # ~56, collapses at page 2
- company: WhatJobs ZA — Java developers
  board: "java developer" # ~52, collapses at page 2
