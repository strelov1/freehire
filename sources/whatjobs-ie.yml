# WhatJobs — Ireland publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# IE account.
#
# The publisher id is NOT here: it lives in the "ie:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-ie" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs IE — Backend developers
  board: "backend developer" # ~247, collapses at page 6
- company: WhatJobs IE — Data engineers
  board: "data engineer" # ≥264 through page 8 tested, still going
- company: WhatJobs IE — Python developers
  board: "python developer" # ~161, collapses at page 5
- company: WhatJobs IE — DevOps engineers
  board: "devops engineer" # ~71, collapses at page 2
- company: WhatJobs IE — Frontend developers
  board: "frontend developer" # ~84, collapses at page 3
- company: WhatJobs IE — Full stack developers
  board: "full stack developer" # ~65, collapses at page 2
- company: WhatJobs IE — Java developers
  board: "java developer" # ~57, collapses at page 2
