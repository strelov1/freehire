# WhatJobs — Mexico publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# MX account.
#
# The publisher id is NOT here: it lives in the "mx:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-mx" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs MX — Full stack developers
  board: "full stack developer" # ≥398 corroborated through page 8 tested, still going
- company: WhatJobs MX — Data engineers
  board: "data engineer" # ~400 through page 8 tested, still going
- company: WhatJobs MX — DevOps engineers
  board: "devops engineer" # ~236, collapses at page 6
- company: WhatJobs MX — Backend developers
  board: "desarrollador backend" # ~180, feed runs dry at page 5
- company: WhatJobs MX — Java developers
  board: "java developer" # ~171, collapses at page 5
- company: WhatJobs MX — React developers
  board: "react developer" # ~88, collapses at page 3
- company: WhatJobs MX — Python developers
  board: "python developer" # ~58, collapses at page 2
- company: WhatJobs MX — Backend engineers
  board: "backend developer" # ~55, collapses at page 2
