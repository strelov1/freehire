# WhatJobs — Switzerland publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# CH account.
#
# The publisher id is NOT here: it lives in the "ch:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-ch" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs CH — DevOps engineers
  board: "devops engineer" # ≥351 corroborated through page 8 tested, still going
- company: WhatJobs CH — Data engineers
  board: "data engineer" # ~333, feed runs dry at page 8
- company: WhatJobs CH — Full stack developers
  board: "full stack developer" # ~321, feed runs dry at page 8
- company: WhatJobs CH — Java developers
  board: "java developer" # ~53, collapses at page 2
- company: WhatJobs CH — Software engineers
  board: "softwareentwickler" # ~33, feed runs dry at page 2
- company: WhatJobs CH — Frontend developers
  board: "frontend developer" # ~28, collapses at page 2
- company: WhatJobs CH — Backend developers
  board: "backend developer" # ~24, feed runs dry at page 2
- company: WhatJobs CH — Python developers
  board: "python developer" # ~15, collapses at page 1
