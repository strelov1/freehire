# WhatJobs — Philippines publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# PH account.
#
# The publisher id is NOT here: it lives in the "ph:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-ph" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs PH — Data engineers
  board: "data engineer" # ~178, collapses at page 5
- company: WhatJobs PH — Full stack developers
  board: "full stack developer" # ~34, collapses at page 2
- company: WhatJobs PH — Java developers
  board: "java developer" # ~23, collapses at page 1
- company: WhatJobs PH — DevOps engineers
  board: "devops engineer" # ~13, collapses at page 1
- company: WhatJobs PH — Python developers
  board: "python developer" # ~13, collapses at page 1
- company: WhatJobs PH — Backend developers
  board: "backend developer" # ~6, collapses at page 1
