# WhatJobs — Pakistan publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# PK account.
#
# The publisher id is NOT here: it lives in the "pk:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-pk" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs PK — Data engineers
  board: "data engineer" # ≥324 corroborated through page 8 tested, still going
- company: WhatJobs PK — Python developers
  board: "python developer" # ~254, collapses at page 7
- company: WhatJobs PK — Java developers
  board: "java developer" # ~237, collapses at page 7
- company: WhatJobs PK — Full stack developers
  board: "full stack developer" # ~246, collapses at page 6
- company: WhatJobs PK — Backend developers
  board: "backend developer" # ~218, feed runs dry at page 6
- company: WhatJobs PK — DevOps engineers
  board: "devops engineer" # ~180, collapses at page 5
- company: WhatJobs PK — Frontend developers
  board: "frontend developer" # ~105, collapses at page 4
