# WhatJobs — Saudi Arabia publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# SA account.
#
# The publisher id is NOT here: it lives in the "sa:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-sa" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs SA — Data engineers
  board: "data engineer" # ≥266 corroborated through page 8 tested, still going
- company: WhatJobs SA — Software engineers
  board: "software engineer" # ~195, collapses at page 6
- company: WhatJobs SA — Backend developers
  board: "backend developer" # ~86, feed runs dry at page 3
- company: WhatJobs SA — DevOps engineers
  board: "devops engineer" # ~64, collapses at page 2
- company: WhatJobs SA — Java developers
  board: "java developer" # ~47, collapses at page 2
- company: WhatJobs SA — Frontend developers
  board: "frontend developer" # ~45, collapses at page 2
- company: WhatJobs SA — Python developers
  board: "python developer" # ~21, collapses at page 1
