# WhatJobs — Belgium publisher account. Same CPC-network shape as sources/whatjobs.yml (see its
# header for the keyword-as-board design); this file only carries what differs for the
# BE account.
#
# The publisher id is NOT here: it lives in the "be:" entry of WHATJOBS_PUBLISHER_IDS, the
# single variable shared by every WhatJobs market. Without it the provider is not registered
# and this file fails validation, by design. It is a SEPARATE account from every other
# market's (its own publisher id, its own id space for the tracked-URL posting id), so it
# registers under its own provider name, "whatjobs-be" — see whatjobsMarkets in
# internal/sources/whatjobs.go.
#
# Keyword volumes are CORROBORATED counts measured live on 2026-08-10 — the number of postings
# that actually name the keyword's terms, not the feed's own padded `total` (see
# sources/whatjobs.yml's header for why `total` cannot be trusted).
#
#
- company: WhatJobs BE — Software engineers
  board: "software ontwikkelaar" # ~115 corroborated, collapses at page 3
- company: WhatJobs BE — Data engineers
  board: "data engineer" # ~98, collapses at page 3
- company: WhatJobs BE — Java developers
  board: "java developer" # ~57, collapses at page 2
- company: WhatJobs BE — Full stack developers
  board: "full stack developer" # ~61, collapses at page 2
- company: WhatJobs BE — DevOps engineers
  board: "devops engineer" # ~19, collapses at page 1
- company: WhatJobs BE — Backend developers
  board: "backend developer" # ~14, collapses at page 1
- company: WhatJobs BE — Frontend developers
  board: "frontend developer" # ~9, collapses at page 1
- company: WhatJobs BE — React developers
  board: "react developer" # ~10, collapses at page 1
