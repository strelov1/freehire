// Code generated by cmd/gen-contracts. DO NOT EDIT.
// Regenerate with `make gen-contracts`.

/**
 * Enrichment is the typed view of a job's enrichment JSONB payload. JSON keys
 * are snake_case to match the existing jobs JSON tags. The blob maps 1:1 to the
 * future search document.
 */
export interface Enrichment {
  /**
   * Summary is a short, model-written synopsis of the role (1–2 sentences): what
   * the job involves and its core stack. Unlike every other field — which is
   * extracted and omitted when the posting does not state it — this one is
   * SYNTHESIZED, so the prompt asks the model to always produce it. It is served
   * free text (no controlling dictionary), bounded by Sanitize.
   */
  summary?: string;
  /**
   * Work arrangement.
   */
  work_mode?: string; // enum: WorkModeValues
  employment_type?: string; // enum: EmploymentTypeValues
  relocation?: string; // enum: RelocationValues
  visa_sponsorship?: boolean; // pointer: false is meaningful
  /**
   * Location / eligibility. Regions is a remote role's geographic reach — a flat
   * macro-region vocabulary (global / continent-level area; country-level reach
   * lives in Countries). It is meaningful only when WorkMode is "remote". Empty
   * means *unknown*; "global" (open anywhere) is an explicit value, never
   * inferred, so global ≠ unknown.
   */
  regions?: string[]; // enum[]: RegionValues
  countries?: string[]; // enum[]: ISO 3166-1 alpha-2
  cities?: string[]; // free text (not faceted)
  timezone_note?: string; // free text (not faceted)
  /**
   * Compensation.
   */
  salary_min?: number /* int */; // in salary_currency units
  salary_max?: number /* int */; // in salary_currency units
  salary_currency?: string; // ISO 4217 (e.g. USD, EUR)
  salary_period?: string; // enum: SalaryPeriodValues
  /**
   * Requirements / qualifications.
   */
  seniority?: string; // enum: SeniorityValues
  experience_years_min?: number /* int */; // non-negative
  english_level?: string; // enum: EnglishLevelValues
  education_level?: string; // enum: EducationLevelValues
  skills?: string[]; // normalized lowercase tokens
  /**
   * Classification.
   */
  category?: string; // enum: CategoryValues
  domains?: string[]; // enum[]: DomainValues
  posting_language?: string; // ISO 639-1 (e.g. en, uk, ru)
  /**
   * Company descriptors (job-time observation; seam to the companies entity).
   */
  company_type?: string; // enum: CompanyTypeValues
  company_size?: string; // enum: CompanySizeValues
  /**
   * Stated requirements (job-only, no CV). Freeform: what the posting itself asks
   * for, with no comparison against any candidate — unlike, and unrelated to,
   * matchanalysis.Requirement, which additionally classifies a requirement against
   * one candidate's résumé. Priority is deliberately NOT schema-constrained (see
   * requestSchema in schema.go): llmschema.Enum only reaches a top-level field or
   * an array of scalars, not a nested property of an array of objects — adding an
   * Enum("requirements", ...) override here would silently produce an inert/wrong
   * constraint rather than erroring, so priority correctness relies on the prompt
   * plus Sanitize's coercion below instead.
   */
  requirements?: Requirement[];
}
/**
 * Requirement is one requirement stated in the posting — job-only, not compared
 * against any CV. (Distinct from, and unrelated to, matchanalysis.Requirement,
 * which additionally classifies a requirement against a specific candidate's
 * résumé.)
 */
export interface Requirement {
  text: string;
  priority: string; // "required" or "preferred"
}

/**
 * Card is the list-row projection of a job: what a card draws, and nothing else.
 * It lives beside Job rather than in the package that lists, because both answer the same
 * question — what of a posting is public — and answering it in two places is how the wire
 * shapes drift. Card is a strict subset of Job's fields, with the same names and the same
 * derivations, so a row and a detail page cannot disagree about a job's work mode or where it
 * is open.
 * The difference is what is absent. Card has no description, and the tracking listing's query
 * does not read one: over 500 applications the descriptions were 2.37 MB of a 2.83 MB response
 * and 13 ms of a 40 ms query, for text no list row renders. A caller that wants the posting
 * reads the one application it opened.
 */
export interface Card {
  public_slug: string;
  title: string;
  company: string;
  closed_at?: string;
  /**
   * The stated facets of the tag row. Empty means unstated, and the row simply omits it —
   * the same rule the full projection follows.
   */
  work_mode?: string;
  seniority?: string;
  employment_type?: string;
  countries?: string[];
  regions?: string[];
  skills?: string[];
  collections?: string[];
  /**
   * PostedAt is the effective posting date, the same derivation the full projection makes:
   * the stated date, or the row's creation when the source never gave one.
   */
  posted_at?: string;
  /**
   * Blurb is the line a row shows under the title — the enrichment's summary, or the
   * opening of the description with its tags stripped. Cut server-side: a row displays
   * about 220 characters, and shipping the whole posting to reach them was 84% of this
   * listing's payload.
   */
  blurb?: string;
}

//////////
// source: ghost.go

/**
 * Ghost is the served ghost signal: a hedged level, the criteria that produced it,
 * and the denominator of the scale the interface shows. It carries facts so the UI
 * can state them, never a bare accusation — the same doctrine as Reality.
 * Contributors is the number of DISTINCT people behind the outcome criteria, and it
 * is ABSENT below the anonymity gate rather than zeroed or rounded. Absence is what
 * makes the guarantee structural: with a single witness there is no count to serve,
 * so no later caller can forget to redact one. (A count of one would identify that
 * applicant to the employer.)
 * The signal is computed on read and never stored — it is time-dependent, and a
 * stored level would go stale sitting still.
 */
export interface Ghost {
  level: string;
  criteria: string[];
  criteria_total: number /* int */;
  contributors?: number /* int */;
  ats_checked_at?: string;
}

//////////
// source: jobview.go
/*
Package jobview defines the single public wire shape of a job — the JSON
representation served by the list, detail, and search endpoints and stored in
the search index. Keeping one type (instead of parallel per-endpoint structs)
makes drift between the API surfaces impossible.
*/

/**
 * Job is the public wire shape of a job. It carries the public_slug and
 * deliberately omits the internal numeric id, which must never be exposed: the
 * id is enumerable and its growth leaks inventory size and fill rate.
 * Enrichment is nested (not flattened) and typed; an unenriched job serializes
 * it as `{}`. Timestamps are RFC3339 UTC strings (or null) — the lexicographic
 * order is chronological, which the search index relies on for sorting.
 */
export interface Job {
  public_slug: string;
  source: string;
  /**
   * ManuallyAdded marks a hand-curated posting added by a moderator (created_by is
   * set), as opposed to an automated source. It is the provenance signal, decoupled
   * from Source (which is the real origin); the authoring user id itself is internal
   * and never exposed.
   */
  manually_added: boolean;
  external_id: string;
  url: string;
  title: string;
  company: string;
  company_slug: string;
  location: string;
  description: string;
  /**
   * WorkMode/Skills are served from the jobs dictionary columns ONLY — the
   * deterministic dictionaries are the sole production source for these facets. The
   * LLM's enrichment values for them are deliberately excluded from the served object
   * (they remain raw in the stored enrichment JSONB), so the LLM can later run free as
   * a discovery signal without corrupting production data.
   * Countries/Regions are a HYBRID facet, like Cities (see geoFacet): the dictionary
   * columns win whenever they pinned a place, but an unpinned geography (no country,
   * and at most the bare-"Remote" "global" bucket) falls back to the LLM's
   * enrichment.countries/regions — catching a restriction stated only in the prose
   * ("Remote (SPAIN only)") that the location string never carried.
   * All four are served top-level and once; the same fields are folded out of the
   * nested Enrichment to avoid duplication.
   */
  countries: string[];
  regions: string[];
  work_mode?: string;
  skills: string[];
  /**
   * Cities is a HYBRID facet (the one deliberate dict+LLM exception): the
   * deterministic jobs.cities column when it resolved a beacon city, otherwise a
   * normalized fallback to the LLM's enrichment.cities — cities are high-cardinality
   * and the dictionary is only a beacon list, so a pure-dict facet would be too
   * sparse. Values are canonical display names (Title case); the LLM copy is folded
   * out of the nested Enrichment like the other geography facets.
   */
  cities: string[];
  /**
   * Collections is the set of curated-collection slugs (e.g. yc, bigtech) the
   * job's company belongs to, denormalized from the company onto the job. It is a
   * deterministic source fact (no LLM counterpart) served straight from the jobs
   * column; an untagged job serializes it as [].
   */
  collections: string[];
  /**
   * IsTech is the deterministic technical/non-technical facet: "tech" or "non_tech",
   * omitted when unknown (the tri-state jobs.is_tech NULL). Served top-level and
   * indexed as a filterable Meili facet; an unknown value is absent so it filters as
   * empty. Derived from title + category (jobderive), never from the LLM.
   */
  is_tech?: string;
  /**
   * RequiresClearance marks a posting that states a government security-clearance
   * requirement (UK SC/DV, US Secret/TS-SCI, AU NV1). Deterministic, derived from
   * the description (internal/dict/location), never from the LLM.
   * A plain bool with omitempty rather than the tri-state IsTech renders as: the
   * stored column only ever holds true or NULL, so the key is present exactly when
   * a requirement was detected. There is no serialized false, because "we detected
   * nothing" must not reach a reader as "this job needs no clearance".
   */
  requires_clearance?: boolean;
  posted_at?: string;
  created_at?: string;
  updated_at?: string;
  /**
   * LastSeenAt is when a re-crawl last confirmed this posting still live — see
   * docs/agents/job-lifecycle.md. The SPA uses it to estimate a rolling
   * JobPosting.validThrough for an open job (seo.ts), since most sources carry
   * no real listing-expiry date of their own.
   */
  last_seen_at?: string;
  /**
   * ClosedAt is non-null when the posting is no longer open. Lists and the
   * search index never contain closed jobs; only the detail endpoint serves
   * them, and the SPA renders the closed state from this field.
   */
  closed_at?: string;
  enrichment: Enrichment;
  enriched_at?: string;
  enrichment_version: number /* int32 */;
  /**
   * ViewCount/AppliedCount are the job's materialized engagement counters —
   * distinct signed-in users who viewed and who marked applied — served straight
   * from the jobs columns (no read-time counting). Displayed on the detail page.
   */
  view_count: number /* int32 */;
  applied_count: number /* int32 */;
  /**
   * UpvoteCount/DownvoteCount are the job's materialized public thumbs counters —
   * distinct signed-in users whose current vote is up / down — served straight from
   * the jobs columns on every read.
   */
  upvote_count: number /* int32 */;
  downvote_count: number /* int32 */;
  /**
   * MyVote is the caller's own vote (-1, 0, 1). It is caller-scoped, so it is only
   * populated on auth-aware detail reads (GetJob with OptionalAuth); list, search,
   * and the index-build path leave it 0 and never persist a real user's vote.
   */
  my_vote: number /* int32 */;
  /**
   * Reality is the job-reality signal (fresh/stale/likely-evergreen + evidence),
   * computed at index/read time and attached by ClassifyReality — never stored, as
   * it is time-dependent. Nil when not computed (e.g. a plain FromRow without counts).
   */
  reality?: Reality;
  /**
   * Ghost is the ghost-job signal (level + the criteria that fired + the scale's
   * denominator), computed at read time by ClassifyGhost — never stored, and never
   * present on a closed job. Nil at level `none`, which is most of the catalogue:
   * the field appears only when the system has something to say.
   */
  ghost?: Ghost;
  /**
   * ReferralAvailable is true when the job's company has at least one approved employee
   * referrer, so the detail page can show the "ask for a referral" affordance. A
   * read-time projection attached only by the detail handler (omitted on list rows,
   * where FromRow leaves it false).
   */
  referral_available?: boolean;
  /**
   * AutoApplyStatus is the caller's own auto-apply status for this job — "queued" (a
   * live, undecided entry) or "declined" (the candidate's own prior decision, permanent)
   * — or nil for no attempt or an anonymous caller. Caller-scoped like MyVote: only the
   * detail handler overlays it, keyed by (caller_id, job_id), never joined into the
   * primary job query every anonymous reader also pays for.
   * See openspec/changes/auto-apply-submit-trigger.
   */
  auto_apply_status?: string;
}

//////////
// source: reality.go

/**
 * Reality is the served job-reality signal: the class plus the observable facts that
 * produced it, so the UI states facts ("open 240 days · reposted 6×") rather than a
 * bare accusation. It is computed at index/read time (never stored — it is
 * time-dependent) and attached via ClassifyReality; a job with no computed signal
 * omits the field.
 */
export interface Reality {
  class: string;
  age_days: number /* int */;
  repost_count: number /* int */;
  mass_posting_count: number /* int */;
  fake_freshness: boolean;
}

/**
 * BundleCoveredPct is the share of a bundle's member skills a CV must hold for the
 * bundle to count as covered. Tunable.
 */
export const BundleCoveredPct = 50;
/**
 * Bundle is one market skill combination and a CV's coverage of it.
 */
export interface Bundle {
  name: string;
  label: string;
  covered: number /* int */; // member skills the CV holds
  total: number /* int */; // bundle size
  has: boolean; // Covered/Total ≥ BundleCoveredPct
}

/**
 * MaxGaps caps how many gap skills the verdict carries — the biggest wins, not an
 * exhaustive list. TopSkills caps the role-skill breakdown at the most in-demand.
 */
export const MaxGaps = 20;
/**
 * MaxGaps caps how many gap skills the verdict carries — the biggest wins, not an
 * exhaustive list. TopSkills caps the role-skill breakdown at the most in-demand.
 */
export const TopSkills = 20;
/**
 * MustHavePct is the vacancy-frequency threshold (percent of the role's open
 * vacancies) at or above which a skill is flagged must-have. Tunable — calibrate
 * against real role distributions.
 */
export const MustHavePct = 50;
/**
 * Skill statuses relative to the CV's parsed skill sets.
 */
export const StatusStrong = "strong"; // declared in the CV's Skills section
/**
 * Skill statuses relative to the CV's parsed skill sets.
 */
export const StatusHidden = "hidden"; // used in the body but not declared
/**
 * Skill statuses relative to the CV's parsed skill sets.
 */
export const StatusAdjacent = "adjacent"; // not held, but a substitutable/transferable skill is
/**
 * Skill statuses relative to the CV's parsed skill sets.
 */
export const StatusMissing = "missing"; // absent from the CV, no close skill held
/**
 * Verdict is the coverage result. JSON is the wire contract shared with the
 * frontend (generated to TS via cmd/gen-contracts).
 */
export interface Verdict {
  total: number /* int64 */; // open vacancies in the role
  covered: number /* int64 */; // vacancies listing ≥1 of the candidate's skills
  coverage_percent: number /* int */; // round(covered / total × 100)
  gaps: Gap[]; // missing skills, biggest win first
  /**
   * Market-anchored role-skill breakdown (all deterministic).
   */
  skills: SkillRow[];
  must_have_total: number /* int */;
  must_have_covered: number /* int */;
  stack_match_percent: number /* int */;
  coherence_percent: number /* int */;
  bundles: Bundle[]; // market skill-combination coverage
}
/**
 * Gap is one missing in-demand skill and the new vacancies it would unlock.
 */
export interface Gap {
  name: string;
  new_vacancies: number /* int64 */; // uncovered vacancies listing this skill
  unlock_percent: number /* int */; // round(new_vacancies / total × 100)
}
/**
 * SkillRow is one of the role's top in-demand skills scored against the CV.
 */
export interface SkillRow {
  name: string;
  market_frequency: number /* int */; // round(vacancies listing it / total × 100)
  must_have: boolean;
  status: string; // strong | hidden | adjacent | missing
  adjacent?: string; // the close skill held, when status is adjacent
  advice: string; // empty for strong
}

/**
 * Status is a check outcome.
 */
export type Status = string;
export const StatusPass: Status = "pass";
export const StatusWarn: Status = "warn";
export const StatusFail: Status = "fail";
/**
 * LineItem is one attributed reason inside a category: awarded points when it
 * passed, recoverable points when it did not.
 */
export interface LineItem {
  points: number /* int */;
  text: string;
  status: Status;
}
/**
 * ScoreCategory is one weighted scoring dimension.
 */
export interface ScoreCategory {
  id: string;
  label: string;
  score: number /* int */; // Max − recoverable points
  max: number /* int */;
  items: LineItem[];
}
/**
 * Report is the full ATS-readiness result. JSON is the wire contract shared with
 * the frontend (an optional LLM layer refines Content Quality; see analyzer.go).
 */
export interface Report {
  overall: number /* int */; // sum of category scores, 0-100
  potential: number /* int */; // overall + recoverable points, capped 100
  categories: ScoreCategory[];
  strong_keywords: string[];
  recommended_keywords: string[];
  /**
   * Reviewed is true once an LLM review has been folded in (see ApplyReview) — the
   * SPA reads it to switch the "Run" button to "Re-run".
   */
  reviewed: boolean;
  /**
   * Suggestions is set only when the optional LLM review ran (see ApplyReview);
   * empty renders no suggestions section.
   */
  suggestions?: string[];
}

//////////
// source: delta.go

/**
 * Delta is the change one CV's ATS readiness shows against another's: the overall
 * move plus one entry per category, and — only when the overall fell — the category
 * that fell furthest. It compares two Reports and computes nothing itself, so the
 * five categories, their labels and their weights stay the scorer's business.
 * JSON is the wire contract shared with the frontend (see cmd/gen-contracts).
 */
export interface Delta {
  base: number /* int */; // the baseline CV's overall score
  tailored: number /* int */; // the compared CV's overall score
  change: number /* int */; // Tailored − Base, signed
  /**
   * Categories reports only the categories present in BOTH reports: a category on
   * one side alone is not a difference, and reporting it against an implied zero
   * would invent a change nobody made.
   */
  categories: CategoryChange[];
  /**
   * Regressed is true when the overall score fell. A category may fall while the
   * overall holds; that is not a regression, and WorstCategory stays empty for it.
   */
  regressed: boolean;
  /**
   * WorstCategory is the id of the category with the most negative change, set only
   * when Regressed. An equal drop resolves to the earlier of the two in the compared
   * report's own order, so the answer never depends on map iteration.
   */
  worst_category?: string;
}
/**
 * CategoryChange is one scoring category's move between the two reports.
 */
export interface CategoryChange {
  id: string;
  label: string;
  base: number /* int */;
  tailored: number /* int */;
  change: number /* int */; // Tailored − Base, signed
  /**
   * Items are the checks behind the TAILORED side's score, so a panel can expand a row
   * into why it stands where it does. The base side's items are deliberately not carried:
   * the candidate is editing the tailored copy, and a before/after list of individual
   * checks is a diff nobody asked for.
   */
  items?: LineItem[];
}

/**
 * Coverage is a requirement's re-derived outcome. Unverifiable is not a middle grade
 * between the other two: it means the requirement named no canonical skill, so the
 * document cannot be checked against it either way.
 */
export type Coverage = string;
export const Covered: Coverage = "covered";
export const Missing: Coverage = "missing";
export const Unverifiable: Coverage = "unverifiable";
/**
 * Category IDs. Exported: the SPA keys its rows by them.
 */
export const CategoryRequirements = "requirements_coverage";
/**
 * Category IDs. Exported: the SPA keys its rows by them.
 */
export const CategoryKeyword = "keyword_match";
/**
 * Category IDs. Exported: the SPA keys its rows by them.
 */
export const CategoryTitle = "job_title_match";
/**
 * Category IDs. Exported: the SPA keys its rows by them.
 */
export const CategorySeniority = "seniority_fit";
/**
 * Category weights, out of 100. They travel in the response rather than being re-declared
 * by the client, so a re-weighting is a server change and the panel's impact labelling can
 * never disagree with the score it labels.
 */
export const WeightRequirements = 40;
/**
 * Category weights, out of 100. They travel in the response rather than being re-declared
 * by the client, so a re-weighting is a server change and the panel's impact labelling can
 * never disagree with the score it labels.
 */
export const WeightKeyword = 30;
/**
 * Category weights, out of 100. They travel in the response rather than being re-declared
 * by the client, so a re-weighting is a server change and the panel's impact labelling can
 * never disagree with the score it labels.
 */
export const WeightTitle = 20;
/**
 * Category weights, out of 100. They travel in the response rather than being re-declared
 * by the client, so a re-weighting is a server change and the panel's impact labelling can
 * never disagree with the score it labels.
 */
export const WeightSeniority = 10;
/**
 * ScoredCategory is one weighted scoring dimension. An unavailable category carries a reason and
 * no items, and is excluded from both sides of the overall — see Score.
 */
export interface ScoredCategory {
  id: string;
  label: string;
  earned: number /* int */;
  weight: number /* int */;
  available: boolean;
  reason?: string;
  items?: LineItem[];
}
/**
 * RequirementCheck is one posting requirement re-derived against the current document.
 * Text and Priority come from the cached fit analysis — they describe the posting and do
 * not depend on any CV. Coverage does not: it is recomputed here, because the cached
 * status was determined against the candidate's base profile.
 */
export interface RequirementCheck {
  text: string;
  priority: string; // required | preferred
  coverage: Coverage;
  /**
   * Skills are the canonical skills the requirement's own text yielded. Empty means the
   * requirement is unverifiable.
   */
  skills?: string[];
  /**
   * Missing are the Skills the document does not contain.
   */
  missing?: string[];
  /**
   * CachedStatus is the status the earlier analysis recorded, carried ONLY for an
   * unverifiable requirement so the panel can show it labelled as coming from that
   * analysis. A re-derived requirement never carries it: a stale verdict beside a live
   * one is worse than no verdict.
   */
  cached_status?: string;
}
/**
 * Score is the full job-match result.
 */
export interface Score {
  overall: number /* int */;
  categories: ScoredCategory[];
  /**
   * Contributing names the categories the overall was computed over, so a score taken
   * across three categories is never mistaken for one taken across four.
   */
  contributing: string[];
  matched_skills?: string[];
  missing_skills?: string[];
  requirements?: RequirementCheck[];
}

/**
 * AdjacentSkill is a job skill the profile does not hold exactly but for which it
 * holds a substitutable neighbour; Via names that held neighbour.
 */
export interface AdjacentSkill {
  name: string;
  via: string;
}
/**
 * JobMatch is the per-job match. Matched, Adjacent, and Missing preserve the job's
 * skill order within each group; CoveragePercent weighs an exact match as 1 and an
 * adjacent match as one half.
 */
export interface JobMatch {
  total: number /* int */;
  exact_count: number /* int */;
  adjacent_count: number /* int */;
  coverage_percent: number /* int */;
  matched: string[];
  adjacent: AdjacentSkill[];
  missing: string[];
}

/**
 * BlockerCategory names the requirement axis a blocker covers.
 */
export type BlockerCategory = string;
export const CategoryExperience: BlockerCategory = "experience";
export const CategoryEducation: BlockerCategory = "education";
export const CategoryLanguage: BlockerCategory = "language";
export const CategoryWorkAuth: BlockerCategory = "work_authorization";
export const CategoryLocationWorkMode: BlockerCategory = "location_work_mode";
export const CategoryCertification: BlockerCategory = "certification";
/**
 * BlockerSeverity grades how hard a blocker is: legal/binary constraints are hard, fit
 * constraints are soft.
 */
export type BlockerSeverity = string;
export const SeverityHard: BlockerSeverity = "hard";
export const SeverityMedium: BlockerSeverity = "medium";
export const SeveritySoft: BlockerSeverity = "soft";
/**
 * Blocker is one evaluated requirement. Met is true when the résumé satisfies it
 * (kept so the UI can show a ✓); only Met==false entries count toward the cap.
 */
export interface Blocker {
  category: BlockerCategory;
  severity: BlockerSeverity;
  score_cap: number /* int */;
  reason: string;
  action: string;
  met: boolean;
}

/**
 * Canonical dimension keys, in the fixed display/scoring order. The model returns
 * the six scores by name; the wire Dimensions slice is built here so a dropped,
 * reordered, or injected dimension can never reach the response.
 */
export const DimTitleAlignment = "title_alignment";
/**
 * Canonical dimension keys, in the fixed display/scoring order. The model returns
 * the six scores by name; the wire Dimensions slice is built here so a dropped,
 * reordered, or injected dimension can never reach the response.
 */
export const DimExperienceRelevance = "experience_relevance";
/**
 * Canonical dimension keys, in the fixed display/scoring order. The model returns
 * the six scores by name; the wire Dimensions slice is built here so a dropped,
 * reordered, or injected dimension can never reach the response.
 */
export const DimSeniorityFit = "seniority_fit";
/**
 * Canonical dimension keys, in the fixed display/scoring order. The model returns
 * the six scores by name; the wire Dimensions slice is built here so a dropped,
 * reordered, or injected dimension can never reach the response.
 */
export const DimSkillsCoverage = "skills_coverage";
/**
 * Canonical dimension keys, in the fixed display/scoring order. The model returns
 * the six scores by name; the wire Dimensions slice is built here so a dropped,
 * reordered, or injected dimension can never reach the response.
 */
export const DimCompanyContext = "company_context";
/**
 * Canonical dimension keys, in the fixed display/scoring order. The model returns
 * the six scores by name; the wire Dimensions slice is built here so a dropped,
 * reordered, or injected dimension can never reach the response.
 */
export const DimLocationFit = "location_fit";
/**
 * Verdict labels (server-derived from overall_score — never taken from the model).
 */
export const VerdictStrong = "Strong Fit";
/**
 * Verdict labels (server-derived from overall_score — never taken from the model).
 */
export const VerdictGood = "Good Fit";
/**
 * Verdict labels (server-derived from overall_score — never taken from the model).
 */
export const VerdictModerate = "Moderate Fit";
/**
 * Verdict labels (server-derived from overall_score — never taken from the model).
 */
export const VerdictWeak = "Weak Fit";
/**
 * Verdict labels (server-derived from overall_score — never taken from the model).
 */
export const VerdictPoor = "Poor Fit";
/**
 * Requirement priorities and match statuses (the ATS lens vocabulary).
 */
export const PriorityRequired = "required";
/**
 * Requirement priorities and match statuses (the ATS lens vocabulary).
 */
export const PriorityPreferred = "preferred";
/**
 * Requirement priorities and match statuses (the ATS lens vocabulary).
 */
export const StatusCovered = "covered"; // present in the CV (verbatim or trivial inflection)
/**
 * Requirement priorities and match statuses (the ATS lens vocabulary).
 */
export const StatusSynonymOnly = "synonym-only"; // the concept is present under a different term
/**
 * Requirement priorities and match statuses (the ATS lens vocabulary).
 */
export const StatusMissingHave = "missing-have"; // profile evidences it but the CV never states it
/**
 * Requirement priorities and match statuses (the ATS lens vocabulary).
 */
export const StatusMissingGap = "missing-gap"; // a genuine gap — absent, no close equivalent held
/**
 * Evidence-strength grades for a covered/synonym-only requirement, strongest first.
 * They rank how firmly the CV backs the match, so the audit and the served verdict
 * can tell real ownership from a bare keyword. The two missing-* statuses carry none.
 */
export const StrengthMetric = "metric"; // an accomplishment with a number, scale, or measured outcome
/**
 * Evidence-strength grades for a covered/synonym-only requirement, strongest first.
 * They rank how firmly the CV backs the match, so the audit and the served verdict
 * can tell real ownership from a bare keyword. The two missing-* statuses carry none.
 */
export const StrengthScope = "scope"; // breadth of work: teams, systems, regions
/**
 * Evidence-strength grades for a covered/synonym-only requirement, strongest first.
 * They rank how firmly the CV backs the match, so the audit and the served verdict
 * can tell real ownership from a bare keyword. The two missing-* statuses carry none.
 */
export const StrengthResponsibility = "responsibility"; // clear ownership with tools or methods
/**
 * Evidence-strength grades for a covered/synonym-only requirement, strongest first.
 * They rank how firmly the CV backs the match, so the audit and the served verdict
 * can tell real ownership from a bare keyword. The two missing-* statuses carry none.
 */
export const StrengthKeyword = "keyword"; // the term is present but the evidence is a bare mention or duty-only
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxCommentRunes = 240; // per-dimension Comment
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxListItemRunes = 200; // each Strengths / Gaps bullet
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxRecommendRunes = 1200; // free-text Recommendation: two or three short prose paragraphs
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxReqTextRunes = 200; // Requirement.Text
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxReqEvidenceRunes = 240; // Requirement.Evidence
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxStrengths = 6; // Strengths list length
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxGaps = 6; // Gaps list length
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxRequirements = 30; // RequirementMatch list length
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxSignals = 5; // HiddenSignals list length
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxSignalQuoteRunes = 200; // Signal.Quote
/**
 * Default sanitize caps for untrusted model text. Override at process start via
 * SetBounds (cmd/server reads MATCH_ANALYSIS_* from the environment). A typo that
 * yields a non-positive value falls back to the matching default so a bound can
 * never be erased.
 */
export const DefaultMaxSignalInsightRunes = 200; // Signal.Insight
/**
 * Bounds is the tunable sanitize ceiling for fit-analysis model output. Every field
 * is a positive count (runes for text, items for lists). Zero / negative values are
 * rejected by SetBounds and replaced with that field's Default*.
 */
export interface Bounds {
  MaxCommentRunes: number /* int */;
  MaxListItemRunes: number /* int */;
  MaxRecommendRunes: number /* int */;
  MaxReqTextRunes: number /* int */;
  MaxReqEvidenceRunes: number /* int */;
  MaxStrengths: number /* int */;
  MaxGaps: number /* int */;
  MaxRequirements: number /* int */;
  MaxSignals: number /* int */;
  MaxSignalQuoteRunes: number /* int */;
  MaxSignalInsightRunes: number /* int */;
}
/**
 * Dimension is one scored fit dimension on the wire.
 */
export interface Dimension {
  key: string;
  label: string;
  score: number /* int */; // 0-100
  comment: string;
}
/**
 * Requirement is one vacancy requirement classified against the CV (the ATS lens).
 */
export interface Requirement {
  text: string;
  priority: string; // required | preferred
  status: string; // covered | synonym-only | missing-have | missing-gap
  evidence: string; // where it appears in the CV, or why it is absent
  evidence_strength: string; // metric|scope|responsibility|keyword for positive statuses; empty for missing-*
}
/**
 * Signal is one interpretive read of the job posting's own wording — a verbatim quote plus
 * what it implies about pace, ownership expectations, team stage, or culture. Freeform text
 * bounded by length only (the same tier as Recommendation/comment), not a controlled
 * vocabulary: there is no fixed set of "signal types" to coerce into.
 */
export interface Signal {
  quote: string;
  insight: string;
}
/**
 * Analysis is the full served fit verdict — the single wire contract exported to TS
 * via cmd/gen-contracts.
 */
export interface Analysis {
  dimensions: Dimension[];
  requirement_match: Requirement[];
  hidden_signals: Signal[];
  overall_score: number /* int */;
  verdict: string;
  strengths: string[];
  gaps: string[];
  recommendation: string;
  blockers: Blocker[];
}

/**
 * Band is how long the letter should be. Two are offered rather than a free-form length,
 * because the application forms do not state a limit — no captured apply_forms field carries
 * maxlength — so the numbers are a product decision and a slider over them would be a
 * precision the data does not support.
 */
export type Band = string;
export const BandShort: Band = "short";
export const BandStandard: Band = "standard";
/**
 * Bounds are the server-owned ceilings on what the chain may return. The model is never
 * trusted to observe them; Sanitize enforces them after the fact, the same discipline
 * matchanalysis keeps over its own output.
 */
export interface Bounds {
  /**
   * ShortCeiling and StandardCeiling bound the body in runes, per band.
   */
  ShortCeiling: number /* int */;
  StandardCeiling: number /* int */;
  /**
   * Floor is the shortest body that still counts as a letter. The audit stage cuts, and a
   * stage whose only instruction is to cut can cut to nothing; below this the caller keeps
   * the un-audited draft instead.
   */
  Floor: number /* int */;
  /**
   * MaxCited caps how many atoms one letter may cite. A letter that cites everything cites
   * nothing — the point of the citation list is that a reader can check it.
   */
  MaxCited: number /* int */;
}
/**
 * Letter is one drafted cover letter: the prose, and the banked atoms it stands on.
 */
export interface Letter {
  body: string;
  /**
   * Cited are the atoms the letter's claims rest on, in the order the letter uses them.
   * This is what the UI shows beside the letter and what makes the honesty claim checkable.
   */
  cited_atom_ids: string[];
  /**
   * Language is what the letter was written in, taken from the VACANCY and not from the
   * candidate's profile — the employer is the reader.
   */
  language: string;
}

/**
 * Structured is the typed, sanitized résumé. Every field is optional — a value the CV
 * does not state is left empty rather than invented (the model is prompted accordingly
 * and Sanitize drops empty entries).
 */
export interface Structured {
  full_name?: string;
  headline?: string;
  location?: string;
  email?: string;
  phone?: string;
  summary?: string;
  total_years?: number /* int */;
  experience?: Experience[];
  education?: Education[];
  languages?: string[];
  links?: string[];
  skills?: string[];
  certifications?: string[];
  projects?: Project[];
}
/**
 * Professional is Structured with the contact fields left out — the résumé's substance
 * (what the person has done) without the identity (who they are). It is what goes to a
 * consumer that has no business knowing the candidate's name: the LLM fit chain, and an
 * agent reading the profile over the API.
 * The field set is a whitelist, deliberately: a field later added to Structured is
 * withheld until it is added here too. A blacklist — dropping the four known contact
 * keys — would disclose that new field by default, which is the wrong way round for a
 * projection whose whole job is to hold personal data back.
 */
export interface Professional {
  headline?: string;
  location?: string;
  summary?: string;
  total_years?: number /* int */;
  experience?: Experience[];
  education?: Education[];
  languages?: string[];
  skills?: string[];
  certifications?: string[];
  projects?: Project[];
}
/**
 * Experience is one work-history entry. Start/End are a structured perioddate.PeriodDate
 * (year, optional month) that the model interprets from however the CV prints the
 * range; Current marks an ongoing role instead of a fabricated End. Summary is the
 * role/company one-line context; Highlights are the achievement bullets.
 */
export interface Experience {
  title?: string;
  company?: string;
  location?: string;
  start?: { year: number; month?: number };
  end?: { year: number; month?: number };
  current?: boolean;
  summary?: string;
  highlights?: string[];
  stack?: string[];
}
/**
 * Project is one portfolio/side-project entry.
 */
export interface Project {
  name?: string;
  link?: string;
  highlights?: string[];
}
/**
 * Education is one education entry. Year is the same structured perioddate.PeriodDate as
 * Experience's Start/End (month is rarely stated for a degree, so it is usually
 * year-only, but the type stays the one shared shape rather than a bare int).
 */
export interface Education {
  degree?: string;
  institution?: string;
  year?: { year: number; month?: number };
}

/**
 * AutopilotStatus is what an autopilot run made of one requirement. The vocabulary is
 * fixed and small on purpose: the panel renders each value, and a value it cannot render
 * is worse than a refused write. Nothing is coerced — an unrecognised status is reported
 * back to the model with the valid ones named, which is how it corrects itself mid-turn.
 */
export type AutopilotStatus = string;
/**
 * AutopilotClosedBank: the experience bank already held evidence, and the run
 * rewrote the CV around it.
 */
export const AutopilotClosedBank: AutopilotStatus = "closed_bank";
/**
 * AutopilotClosedCandidate: the candidate confirmed it in conversation after the
 * run, their words were banked, and the bullet cites that record.
 */
export const AutopilotClosedCandidate: AutopilotStatus = "closed_candidate";
/**
 * AutopilotOpen: the bank holds nothing for it. This is the remainder worth asking about.
 */
export const AutopilotOpen: AutopilotStatus = "open";
/**
 * AutopilotNotReached: the run ended (step cap, cancellation) before reaching it.
 */
export const AutopilotNotReached: AutopilotStatus = "not_reached";
/**
 * AutopilotEntry is one requirement and what the run made of it.
 */
export interface AutopilotEntry {
  requirement: string;
  status: AutopilotStatus;
  note?: string;
}

//////////
// source: cv.go
/*
Package cv is the CV-builder domain: the structured CV Document, its sanitizer,
seeding from the extracted résumé, and PDF rendering behind a Renderer interface.
*/

/**
 * DefaultMaxBullets is the per-experience / per-project bullet ceiling when
 * CV_MAX_BULLETS is unset. Writers must refuse growth past MaxBullets rather
 * than let Sanitize drop trailing content silently.
 */
export const DefaultMaxBullets = 20;
/**
 * Document is the typed, sanitized CV. Every field is optional; sections the user has
 * not filled in are left empty rather than invented, and Sanitize drops empty entries.
 */
export interface Document {
  margins: Margins;
  /**
   * No omitempty: it does nothing on a struct (encoding/json only omits false, 0, nil, and
   * empty strings/slices/maps), so the tag would promise clients an absence they never see
   * while making the generated TypeScript optional for no reason.
   */
  style: Style;
  header: Header;
  summary?: string;
  experience?: ExperienceItem[];
  education?: EducationItem[];
  skills?: SkillGroup[];
  languages?: Language[];
  projects?: Project[];
  certifications?: Certification[];
}
/**
 * Margins is the CV's per-side page margin in inches, applied identically to the HTML
 * preview and the Typst-rendered PDF. Sanitize clamps each side and defaults an unset
 * (zero) side to defaultMargin.
 */
export interface Margins {
  top: number /* float64 */;
  right: number /* float64 */;
  bottom: number /* float64 */;
  left: number /* float64 */;
}
/**
 * Style is the CV's typography: the typeface, the base type size in points, and the line
 * height as a leading multiple of the em. It is part of the document, so it persists with
 * the CV, is copied when the CV is tailored, and is not clobbered by field-level patches.
 * A zero value means "whatever the active template uses" and Sanitize KEEPS IT ZERO. This is
 * the opposite of Margins, which resolves an unset side to a concrete 0.5in, and the
 * difference is deliberate on both counts:
 *   - Every CV written before this type existed has no style block, so it renders exactly as
 *     it did. There is nothing to migrate.
 *   - A template stays a whole design choice. Switching to a template with looser leading
 *     actually loosens the leading, because that value was never frozen into the document.
 * Resolving defaults here — the natural thing to do by analogy with clampMargin — would
 * silently bake the current template's typography into every document on its next save.
 * FontFamily is a registry id (see fonts.go), never a renderer's own face name: the renderer
 * resolves it on its own copy of the document at render time.
 */
export interface Style {
  font_family?: string;
  font_size?: number /* float64 */; // points
  line_height?: number /* float64 */; // em of leading
}
/**
 * Header is the top-of-CV contact block. The tagline under the name is Document.Summary
 * (there is no separate headline field — one "summary" term across the CV).
 */
export interface Header {
  full_name?: string;
  email?: string;
  phone?: string;
  location?: string;
  links?: string[];
}
/**
 * Experience is one work-history entry. Start/End are a structured perioddate.PeriodDate
 * (year, optional month) — the same shared type experience.Employment and
 * resumeextract.Experience use. Rendering to PDF formats this back to display text
 * (see renderer.go); no template deals with the structured value directly.
 */
export interface ExperienceItem {
  role?: string;
  company?: string;
  location?: string;
  start?: { year: number; month?: number };
  end?: { year: number; month?: number };
  current?: boolean;
  /**
   * Summary is the one-line company/role context printed under the role header, before
   * the bullets. Stack is the per-role technology line printed after the bullets.
   */
  summary?: string;
  bullets?: string[];
  stack?: string[];
}
/**
 * Education is one education entry. Start/End are the same structured perioddate.PeriodDate.
 */
export interface EducationItem {
  institution?: string;
  degree?: string;
  field?: string;
  start?: { year: number; month?: number };
  end?: { year: number; month?: number };
}
/**
 * SkillGroup is a named cluster of skills (e.g. "Languages" → Go, Python). A group with
 * no items and no name is dropped.
 */
export interface SkillGroup {
  group?: string;
  items?: string[];
}
/**
 * Language is one spoken/written language and its proficiency level.
 */
export interface Language {
  name?: string;
  level?: string;
}
/**
 * Project is one portfolio/side-project entry.
 */
export interface Project {
  name?: string;
  link?: string;
  bullets?: string[];
}
/**
 * Certification is one certification/credential. Year is the same structured
 * perioddate.PeriodDate.
 */
export interface Certification {
  name?: string;
  issuer?: string;
  year?: { year: number; month?: number };
}

/**
 * RevisionView is one entry in the history feed.
 * It deliberately does NOT carry the operations. The feed needs to say what changed and
 * where, not to reproduce it: the addresses are enough to underline the right lines in the
 * preview, while the inverses hold the candidate's own earlier text and have no business
 * leaving the server for a list view.
 */
export interface RevisionView {
  id: string;
  /**
   * Actor is who made the change: the candidate, the agent, or the system.
   */
  actor: string;
  /**
   * Origin is the entry point it arrived through — the editor, the template picker, the
   * tailoring agent, the CLI, an import.
   */
  origin: string;
  /**
   * BatchID groups the edits of one agent turn, so the feed can offer to undo the run as
   * a whole. Empty for a change that stands alone.
   */
  batch_id?: string;
  /**
   * Title describes the change in the application's own words. It is generated from the
   * operations on the server and never written by a model.
   */
  title: string;
  /**
   * Note is the agent's own reason for the edit, when it gave one. It is the model's text
   * and must be rendered as the agent's words, attributed — never as the entry's own
   * description.
   */
  note?: string;
  /**
   * Paths are the places this revision touched, e.g. `experience[2].bullets[1]`. The
   * preview underlines the nodes they address.
   */
  paths: string[];
  /**
   * Reverted says the change has already been undone, which is what greys out its control
   * rather than removing the entry: the log is never rewritten.
   */
  reverted: boolean;
  /**
   * Undoable says the entry has something to reverse. A milestone — "this CV was created"
   * — has not: undoing it would mean deleting the CV, which is a different action with its
   * own button. Stated rather than inferred from an empty path list, because "changed
   * nothing addressable" and "cannot be undone" are different facts.
   */
  undoable: boolean;
  /**
   * RevertsID names the revision this one undid, when it is itself an undo.
   */
  reverts_id?: string;
  created_at: string;
}

/**
 * Display is a captured form shaped for a candidate to read rather than for a
 * machine to submit. It is the near-inverse of what the store keeps: the store holds
 * the platform's identifiers and option values because they exist to be handed back,
 * and a reader wants none of that — only the question and one word about the answer.
 */
export interface Display {
  /**
   * Provider is the platform the form was read from, so a reader can say where the
   * answer came from.
   */
  provider: string;
  /**
   * Basics are the controls every application demands — name, contact details, CV —
   * listed once instead of one entry each. Stated rather than dropped, because a
   * form that does NOT want a CV is worth knowing too.
   */
  basics: string[];
  /**
   * Questions are the employer's own, in the order the form presents them.
   */
  questions: Question[];
}
/**
 * Question is one thing the employer will ask.
 */
export interface Question {
  /**
   * Text is the question as the employer wrote it, unedited.
   */
  text: string;
  /**
   * Required is whether the platform refuses the application without an answer.
   */
  required: boolean;
  /**
   * Answer names the kind of answer expected, empty where naming it would add
   * nothing (a one-line answer is the default expectation) or where the capture
   * could not normalize the control's kind.
   */
  answer?: string;
}

/**
 * Answers is the candidate's stated screening answers. Every field is independently
 * optional: a nil pointer (or a nil/empty slice for AuthorizedCountries) means the
 * candidate has not stated that fact, never a guessed default.
 */
export interface Answers {
  authorized_countries?: string[];
  visa_sponsorship_needed?: boolean;
  desired_salary_amount?: number /* int */;
  desired_salary_currency?: string;
  desired_salary_period?: string;
  notice_period_days?: number /* int */;
  willing_to_relocate?: boolean;
  age_18_or_older?: boolean;
}

/**
 * Responses is one candidate's survey record. Every field is independently optional: a nil
 * pointer means the candidate has not stated that fact, never a guessed default and never
 * a zero standing in for silence.
 */
export interface Responses {
  job_search_stage?: string;
  biggest_challenge?: string;
  /**
   * BiggestChallengeNote is free text, and only vocab.JobChallengeOther admits it. A note
   * beside a coded challenge could contradict it, and nothing downstream would know which
   * to believe.
   */
  biggest_challenge_note?: string;
  current_income_amount?: number /* int */;
  current_income_currency?: string;
  current_income_period?: string;
}

export const SOURCE_VALUES = ['telegram', 'workatastartup', 'remoteok', 'arc', '4dayweek', 'adp', 'adzuna', 'aijobs', 'applicantpro', 'applitrack', 'apploi', 'arbeitnow', 'arbeitsagentur', 'ashby', 'ashbygraphql', 'avature', 'bamboohr', 'bayt', 'betterteam', 'breezy', 'briefhq', 'bullhorn', 'careerplug', 'careerspage', 'catsone', 'cleverstaff', 'clinch', 'comeet', 'compleo', 'cornerstone', 'crelate', 'cryptocurrencyjobs', 'dayforce', 'deel', 'djinni', 'earcu', 'echojobs', 'edjoin', 'eightfold', 'enlizt', 'epam', 'erecruiter', 'factorial', 'freshteam', 'functionalworks', 'geekjob', 'gem', 'getmanfred', 'getmatch', 'getonbrd', 'getro', 'globalpayments', 'greenhouse', 'gulftalent', 'gupy', 'gusto', 'habr_career', 'hh', 'hibob', 'himalayas', 'hireology', 'hiringthing', 'hrmdirect', 'huntflow', 'hurma', 'icims', 'infojobs', 'inhire', 'instaffo', 'ismartrecruit', 'isolvedhire', 'itechart', 'jazzhr', 'jibe', 'jobdanmark', 'jobicy', 'jobnet', 'jobscore', 'jobspresso', 'jobstash', 'jobtech', 'jobvite', 'jobylon', 'join', 'justjoin', 'landingjobs', 'lever', 'likeit', 'loxo', 'luxoft', 'manatal', 'mindsight', 'mycareersfuture', 'neogov', 'nodesk', 'nofluffjobs', 'northstone', 'odoo', 'opencats', 'oracle', 'pageup', 'paycom', 'paycor', 'paylocity', 'peopleforce', 'personio', 'phenom', 'pinpoint', 'powertofly', 'profession', 'quickin', 'radancy', 'rapyd', 'recruitee', 'recruitingsolutions', 'reed', 'remotedotcom', 'remotive', 'remotli', 'rippling', 'schoolspring', 'seek', 'senior', 'smartrecruiters', 'softgarden', 'solides', 'solidjobs', 'spark', 'speedrun', 'startupandvc', 'successfactors', 'talentadore', 'talenthr', 'talentlyft', 'taleo', 'teamex', 'teamtailor', 'tecla', 'thehub', 'themuse', 'topco', 'traffit', 'trakstar', 'trudvsem', 'tyomarkkinatori', 'ukg', 'ukgready', 'usajobs', 'vagas', 'vention', 'vouch', 'wantapply', 'wantedkr', 'weworkremotely', 'whatjobs', 'whatjobs-ae', 'whatjobs-ar', 'whatjobs-at', 'whatjobs-au', 'whatjobs-be', 'whatjobs-bh', 'whatjobs-br', 'whatjobs-ca', 'whatjobs-ch', 'whatjobs-cl', 'whatjobs-co', 'whatjobs-de', 'whatjobs-dk', 'whatjobs-eg', 'whatjobs-es', 'whatjobs-fi', 'whatjobs-fr', 'whatjobs-gr', 'whatjobs-hk', 'whatjobs-hu', 'whatjobs-id', 'whatjobs-ie', 'whatjobs-in', 'whatjobs-it', 'whatjobs-ke', 'whatjobs-kw', 'whatjobs-lu', 'whatjobs-mx', 'whatjobs-my', 'whatjobs-nl', 'whatjobs-no', 'whatjobs-nz', 'whatjobs-om', 'whatjobs-pe', 'whatjobs-ph', 'whatjobs-pk', 'whatjobs-pl', 'whatjobs-pt', 'whatjobs-py', 'whatjobs-qa', 'whatjobs-sa', 'whatjobs-se', 'whatjobs-sg', 'whatjobs-sv', 'whatjobs-th', 'whatjobs-tr', 'whatjobs-uk', 'whatjobs-ve', 'whatjobs-vn', 'whatjobs-za', 'workable', 'workablemarketplace', 'workday', 'workingnomads', 'workstream', 'wpyoast', 'zohorecruit'] as const;
export type Source = (typeof SOURCE_VALUES)[number];
export const STAGE_VALUES = ['preparing', 'applied', 'screening', 'responded', 'interview', 'offer', 'accepted', 'rejected', 'withdrawn', 'expired'] as const;
export type Stage = (typeof STAGE_VALUES)[number];
export const APPLICATION_EVENT_KINDS = ['applied', 'employer_reply', 'follow_up_sent', 'stage_set', 'interview_scheduled'] as const;
export type ApplicationEventKind = (typeof APPLICATION_EVENT_KINDS)[number];
export const STAGE_LABELS = {
  'accepted': 'Accepted',
  'applied': 'Applied',
  'expired': 'Expired',
  'interview': 'Interview',
  'offer': 'Offer',
  'preparing': 'Preparing',
  'rejected': 'Rejected',
  'responded': 'Responded',
  'screening': 'Screening',
  'withdrawn': 'Withdrawn',
} as const;
export type StageLabels = typeof STAGE_LABELS;
export const STAGE_GROUPS = [
  { id: 'preparing', label: 'Preparing', stages: ['preparing'] },
  { id: 'applied', label: 'Applied', stages: ['applied', 'screening', 'responded'] },
  { id: 'interview', label: 'Interview', stages: ['interview'] },
  { id: 'offer', label: 'Offer', stages: ['offer'] },
  { id: 'closed', label: 'Closed', stages: ['accepted', 'rejected', 'withdrawn', 'expired'] },
] as const;
export type StageGroup = (typeof STAGE_GROUPS)[number];
export const WORK_MODE_VALUES = ['remote', 'hybrid', 'onsite'] as const;
export type WorkMode = (typeof WORK_MODE_VALUES)[number];
export const SENIORITY_VALUES = ['intern', 'junior', 'middle', 'senior', 'lead', 'staff', 'principal', 'c_level'] as const;
export type Seniority = (typeof SENIORITY_VALUES)[number];
export const CATEGORY_VALUES = ['software_engineering', 'backend', 'frontend', 'fullstack', 'mobile', 'devops', 'sre', 'network_engineering', 'data_engineering', 'data_science', 'data_analytics', 'ml_ai', 'ai_engineering', 'qa', 'security', 'hardware', 'embedded', 'blockchain', 'architecture', 'design', 'creative', 'engineering_design', 'industrial_engineering', 'product', 'project_management', 'management', 'healthcare', 'skilled_trades', 'retail', 'hospitality', 'logistics', 'education', 'personal_services', 'administration', 'marketing', 'sales', 'support', 'business_analysis', 'solutions_engineering', 'developer_relations', 'technical_writing', 'recruiting', 'hr', 'finance', 'legal', 'operations', 'customer_success', 'other'] as const;
export type Category = (typeof CATEGORY_VALUES)[number];
export const EMPLOYMENT_TYPE_VALUES = ['full_time', 'part_time', 'contract', 'internship', 'fellowship'] as const;
export type EmploymentType = (typeof EMPLOYMENT_TYPE_VALUES)[number];
export const RELOCATION_VALUES = ['not_supported', 'supported', 'required'] as const;
export type Relocation = (typeof RELOCATION_VALUES)[number];
export const ENGLISH_LEVEL_VALUES = ['none', 'a1', 'a2', 'b1', 'b2', 'c1', 'c2', 'native'] as const;
export type EnglishLevel = (typeof ENGLISH_LEVEL_VALUES)[number];
export const COMPANY_TYPE_VALUES = ['product', 'startup', 'outsource', 'outstaff', 'agency', 'inhouse', 'government'] as const;
export type CompanyType = (typeof COMPANY_TYPE_VALUES)[number];
export const COMPANY_FEEDBACK_TYPE_VALUES = ['interview', 'culture', 'compensation', 'management', 'work_life_balance', 'career_growth', 'other'] as const;
export type CompanyFeedbackType = (typeof COMPANY_FEEDBACK_TYPE_VALUES)[number];
export const COMPANY_FEEDBACK_REPORT_REASON_VALUES = ['spam', 'offensive', 'false_information', 'other'] as const;
export type CompanyFeedbackReportReason = (typeof COMPANY_FEEDBACK_REPORT_REASON_VALUES)[number];
export const DOMAIN_VALUES = ['fintech', 'crypto', 'ecommerce', 'gambling', 'gamedev', 'media', 'travel', 'healthcare', 'edtech', 'govtech', 'devtools', 'cybersecurity', 'ai', 'hrtech', 'adtech', 'proptech', 'logistics', 'mobility', 'climatetech', 'other'] as const;
export type Domain = (typeof DOMAIN_VALUES)[number];
export const INDUSTRY_VALUES = ['accounting', 'adtech', 'aerospace', 'agriculture', 'ai', 'architecture', 'automotive', 'banking', 'biotech', 'building-materials', 'climate-tech', 'construction', 'consumer-goods', 'crypto', 'cybersecurity', 'data-analytics', 'defense', 'developer-tools', 'digital-marketing', 'ecommerce', 'edtech', 'education', 'energy', 'engineering-services', 'enterprise-software', 'entertainment', 'environmental-services', 'facilities-services', 'financial-services', 'fintech', 'food-and-beverage', 'gambling', 'gaming', 'government', 'health-tech', 'healthcare', 'higher-education', 'home-services', 'hospitality', 'hr-tech', 'industrial-automation', 'insurance', 'iot', 'it-services', 'k-12-education', 'legal-services', 'logistics', 'manufacturing', 'medical-devices', 'mental-health', 'nonprofit', 'oil-and-gas', 'pharmaceuticals', 'property-management', 'proptech', 'public-safety', 'real-estate', 'recruitment', 'regtech', 'religious-organizations', 'renewable-energy', 'retail', 'robotics', 'senior-care', 'social-services', 'software-development', 'technology-consulting', 'telecommunications', 'telehealth', 'transportation', 'travel', 'utilities', 'wealth-management', 'wholesale'] as const;
export type Industry = (typeof INDUSTRY_VALUES)[number];
export const INDUSTRY_LABELS = {
  'accounting': 'Accounting',
  'adtech': 'AdTech',
  'aerospace': 'Aerospace',
  'agriculture': 'Agriculture',
  'ai': 'AI',
  'architecture': 'Architecture',
  'automotive': 'Automotive',
  'banking': 'Banking',
  'biotech': 'Biotech',
  'building-materials': 'Building Materials',
  'climate-tech': 'Climate Tech',
  'construction': 'Construction',
  'consumer-goods': 'Consumer Goods',
  'crypto': 'Crypto',
  'cybersecurity': 'Cybersecurity',
  'data-analytics': 'Data Analytics',
  'defense': 'Defense',
  'developer-tools': 'Developer Tools',
  'digital-marketing': 'Digital Marketing',
  'ecommerce': 'E-commerce',
  'edtech': 'EdTech',
  'education': 'Education',
  'energy': 'Energy',
  'engineering-services': 'Engineering Services',
  'enterprise-software': 'Enterprise Software',
  'entertainment': 'Entertainment',
  'environmental-services': 'Environmental Services',
  'facilities-services': 'Facilities Services',
  'financial-services': 'Financial Services',
  'fintech': 'Fintech',
  'food-and-beverage': 'Food & Beverage',
  'gambling': 'Gambling',
  'gaming': 'Gaming',
  'government': 'Government',
  'health-tech': 'Health Tech',
  'healthcare': 'Healthcare',
  'higher-education': 'Higher Education',
  'home-services': 'Home Services',
  'hospitality': 'Hospitality',
  'hr-tech': 'HR Tech',
  'industrial-automation': 'Industrial Automation',
  'insurance': 'Insurance',
  'iot': 'IoT',
  'it-services': 'IT Services',
  'k-12-education': 'K-12 Education',
  'legal-services': 'Legal Services',
  'logistics': 'Logistics',
  'manufacturing': 'Manufacturing',
  'medical-devices': 'Medical Devices',
  'mental-health': 'Mental Health',
  'nonprofit': 'Nonprofit',
  'oil-and-gas': 'Oil & Gas',
  'pharmaceuticals': 'Pharmaceuticals',
  'property-management': 'Property Management',
  'proptech': 'PropTech',
  'public-safety': 'Public Safety',
  'real-estate': 'Real Estate',
  'recruitment': 'Recruitment',
  'regtech': 'RegTech',
  'religious-organizations': 'Religious Organizations',
  'renewable-energy': 'Renewable Energy',
  'retail': 'Retail',
  'robotics': 'Robotics',
  'senior-care': 'Senior Care',
  'social-services': 'Social Services',
  'software-development': 'Software Development',
  'technology-consulting': 'Technology Consulting',
  'telecommunications': 'Telecommunications',
  'telehealth': 'Telehealth',
  'transportation': 'Transportation',
  'travel': 'Travel',
  'utilities': 'Utilities',
  'wealth-management': 'Wealth Management',
  'wholesale': 'Wholesale',
} as const;
export type IndustryLabels = typeof INDUSTRY_LABELS;
export const AI_ARCHETYPE_VALUES = ['rag_app_builder', 'agent_builder', 'cloud_ml_platform_engineer', 'ml_trainer_researcher', 'fullstack_ai_engineer', 'devops_infra_engineer'] as const;
export type AIArchetype = (typeof AI_ARCHETYPE_VALUES)[number];
export const ROLE_TYPE_VALUES = ['people_manager'] as const;
export type RoleType = (typeof ROLE_TYPE_VALUES)[number];
export const GHOST_CRITERION_VALUES = ['evergreen_posting', 'ats_absent', 'silent_applications', 'user_reports'] as const;
export type GhostCriterion = (typeof GHOST_CRITERION_VALUES)[number];
export const EMAIL_STATUS_SIGNAL_VALUES = ['acknowledgement', 'screening', 'interview_invitation', 'assessment', 'offer', 'rejection', 'info_request', 'incomplete_application', 'other'] as const;
export type EmailStatusSignal = (typeof EMAIL_STATUS_SIGNAL_VALUES)[number];
export const SIGNAL_STAGE = {
  'acknowledgement': { stage: 'applied', advances: true },
  'assessment': { stage: 'screening', advances: true },
  'incomplete_application': { stage: '', advances: false },
  'info_request': { stage: '', advances: false },
  'interview_invitation': { stage: 'interview', advances: true },
  'offer': { stage: 'offer', advances: true },
  'other': { stage: '', advances: false },
  'rejection': { stage: 'rejected', advances: false },
  'screening': { stage: 'screening', advances: true },
} as const;
export type SignalStage = typeof SIGNAL_STAGE;
export const COUNTRY_REGION_MAP = {
  'ad': 'eu',
  'ae': 'mena',
  'al': 'eu',
  'am': 'cis',
  'ao': 'africa',
  'ar': 'latam',
  'at': 'eu',
  'au': 'apac',
  'az': 'cis',
  'ba': 'eu',
  'bd': 'apac',
  'be': 'eu',
  'bg': 'eu',
  'bh': 'mena',
  'bn': 'apac',
  'bo': 'latam',
  'br': 'latam',
  'by': 'cis',
  'ca': 'north_america',
  'ch': 'eu',
  'ci': 'africa',
  'cl': 'latam',
  'cm': 'africa',
  'cn': 'apac',
  'co': 'latam',
  'cr': 'latam',
  'cy': 'eu',
  'cz': 'eu',
  'de': 'eu',
  'dk': 'eu',
  'do': 'latam',
  'dz': 'mena',
  'ec': 'latam',
  'ee': 'eu',
  'eg': 'mena',
  'es': 'eu',
  'et': 'africa',
  'fi': 'eu',
  'fr': 'eu',
  'gb': 'uk',
  'ge': 'cis',
  'gh': 'africa',
  'gr': 'eu',
  'gt': 'latam',
  'hk': 'apac',
  'hn': 'latam',
  'hr': 'eu',
  'hu': 'eu',
  'id': 'apac',
  'ie': 'eu',
  'il': 'mena',
  'in': 'apac',
  'iq': 'mena',
  'ir': 'mena',
  'is': 'eu',
  'it': 'eu',
  'jo': 'mena',
  'jp': 'apac',
  'ke': 'africa',
  'kg': 'cis',
  'kh': 'apac',
  'kr': 'apac',
  'kw': 'mena',
  'kz': 'cis',
  'la': 'apac',
  'lb': 'mena',
  'li': 'eu',
  'lk': 'apac',
  'lt': 'eu',
  'lu': 'eu',
  'lv': 'eu',
  'ly': 'mena',
  'ma': 'mena',
  'mc': 'eu',
  'md': 'cis',
  'me': 'eu',
  'mk': 'eu',
  'mm': 'apac',
  'mn': 'apac',
  'mo': 'apac',
  'mt': 'eu',
  'mu': 'africa',
  'mx': 'latam',
  'my': 'apac',
  'mz': 'africa',
  'ng': 'africa',
  'ni': 'latam',
  'nl': 'eu',
  'no': 'eu',
  'np': 'apac',
  'nz': 'apac',
  'om': 'mena',
  'pa': 'latam',
  'pe': 'latam',
  'ph': 'apac',
  'pk': 'apac',
  'pl': 'eu',
  'pr': 'latam',
  'ps': 'mena',
  'pt': 'eu',
  'py': 'latam',
  'qa': 'mena',
  'ro': 'eu',
  'rs': 'eu',
  'ru': 'cis',
  'rw': 'africa',
  'sa': 'mena',
  'se': 'eu',
  'sg': 'apac',
  'si': 'eu',
  'sk': 'eu',
  'sm': 'eu',
  'sn': 'africa',
  'sv': 'latam',
  'th': 'apac',
  'tj': 'cis',
  'tm': 'cis',
  'tn': 'mena',
  'tr': 'mena',
  'tw': 'apac',
  'tz': 'africa',
  'ua': 'eu',
  'ug': 'africa',
  'us': 'north_america',
  'uy': 'latam',
  'uz': 'cis',
  've': 'latam',
  'vn': 'apac',
  'xk': 'eu',
  'ye': 'mena',
  'za': 'africa',
  'zm': 'africa',
  'zw': 'africa',
} as const;
export type CountryRegionMap = typeof COUNTRY_REGION_MAP;
export const SKILL_LABELS = {
  '1c': '1C',
  '3ds-max': '3ds Max',
  'ab-testing': 'A/B Testing',
  'abap': 'ABAP',
  'acceptance-criteria': 'Acceptance Criteria',
  'accessibility': 'Accessibility',
  'account-executive': 'Account Executive',
  'accounts-payable': 'Accounts Payable',
  'accounts-receivable': 'Accounts Receivable',
  'active-directory': 'Active Directory',
  'activemq': 'ActiveMQ',
  'actix': 'Actix',
  'adobe-analytics': 'Adobe Analytics',
  'adobe-xd': 'Adobe XD',
  'adp': 'Adp',
  'after-effects': 'After Effects',
  'agentic-ai': 'Agentic AI',
  'agile': 'Agile',
  'ahrefs': 'Ahrefs',
  'ai': 'AI',
  'ai-enablement': 'Ai Enablement',
  'airbyte': 'Airbyte',
  'airflow': 'Airflow',
  'airtable': 'Airtable',
  'ajax': 'AJAX',
  'aks': 'AKS',
  'alteryx': 'Alteryx',
  'altium': 'Altium',
  'amplitude': 'Amplitude',
  'analytics': 'Analytics',
  'anaplan': 'Anaplan',
  'android': 'Android',
  'angular': 'Angular',
  'anomaly-detection': 'Anomaly Detection',
  'ansible': 'Ansible',
  'ansys': 'Ansys',
  'anthropic': 'Anthropic',
  'apex': 'Apex',
  'api': 'API',
  'api-design': 'Api Design',
  'api-documentation': 'Api Documentation',
  'api-security': 'Api Security',
  'apollo-io': 'Apollo Io',
  'appium': 'Appium',
  'arcgis': 'ArcGIS',
  'argocd': 'Argo CD',
  'ariba': 'Ariba',
  'artifactory': 'Artifactory',
  'as400': 'AS/400',
  'as9100': 'As9100',
  'asana': 'Asana',
  'asic': 'Asic',
  'assembly': 'Assembly',
  'astro': 'Astro',
  'athena': 'Athena',
  'atlassian': 'Atlassian',
  'aurora': 'Aurora',
  'authentication': 'Authentication',
  'autocad': 'AutoCAD',
  'autodesk-inventor': 'Autodesk Inventor',
  'autogen': 'AutoGen',
  'automation': 'Automation',
  'aws': 'AWS',
  'aws-bedrock': 'AWS Bedrock',
  'aws-glue': 'Aws Glue',
  'azure': 'Azure',
  'azure-ad': 'Azure AD',
  'azure-data-factory': 'Azure Data Factory',
  'azure-devops': 'Azure DevOps',
  'azure-functions': 'Azure Functions',
  'azure-synapse': 'Azure Synapse',
  'backbone': 'Backbone.js',
  'bamboohr': 'BambooHR',
  'bash': 'Bash',
  'bdd': 'BDD',
  'bentoml': 'BentoML',
  'bgp': 'BGP',
  'bicep': 'Bicep',
  'bigquery': 'BigQuery',
  'bioinformatics': 'Bioinformatics',
  'bitbucket': 'Bitbucket',
  'blender': 'Blender',
  'blockchain': 'Blockchain',
  'bluebeam': 'Bluebeam',
  'boolean-search': 'Boolean Search',
  'bootstrap': 'Bootstrap',
  'bpmn': 'BPMN',
  'braze': 'Braze',
  'burp-suite': 'Burp Suite',
  'business-development': 'Business Development',
  'c': 'C',
  'cad': 'CAD',
  'cadence-virtuoso': 'Cadence Virtuoso',
  'can-bus': 'Can Bus',
  'candidate-experience': 'Candidate Experience',
  'canva': 'Canva',
  'capcut': 'Capcut',
  'cassandra': 'Cassandra',
  'catia': 'CATIA',
  'causal-inference': 'Causal Inference',
  'ccna': 'Ccna',
  'ccpa': 'Ccpa',
  'cdk': 'CDK',
  'certified-scrummaster': 'Certified ScrumMaster',
  'cfd': 'Cfd',
  'chatgpt': 'ChatGPT',
  'chromadb': 'ChromaDB',
  'churn-prevention': 'Churn Prevention',
  'churnzero': 'ChurnZero',
  'ci-cd': 'CI/CD',
  'cinema-4d': 'Cinema 4d',
  'circleci': 'CircleCI',
  'cisco': 'Cisco',
  'cism': 'Cism',
  'cissp': 'Cissp',
  'citrix': 'Citrix',
  'civil-3d': 'Civil 3D',
  'claude': 'Claude',
  'claude-code': 'Claude Code',
  'cli': 'CLI',
  'clickhouse': 'ClickHouse',
  'clojure': 'Clojure',
  'cloud': 'Cloud',
  'cloud-native': 'Cloud Native',
  'cloud-security': 'Cloud Security',
  'cloudformation': 'CloudFormation',
  'cloudwatch': 'CloudWatch',
  'cmake': 'CMake',
  'cmdb': 'CMDB',
  'cmmc': 'Cmmc',
  'cmos': 'Cmos',
  'cnc': 'Cnc',
  'cobol': 'COBOL',
  'cockroachdb': 'CockroachDB',
  'cohere': 'Cohere',
  'cold-outreach': 'Cold Outreach',
  'color-grading': 'Color Grading',
  'community-management': 'Community Management',
  'computer-vision': 'Computer Vision',
  'concur': 'Concur',
  'confluence': 'Confluence',
  'container-security': 'Container Security',
  'containerd': 'Containerd',
  'containerization': 'Containerization',
  'content-marketing': 'Content Marketing',
  'contentful': 'Contentful',
  'contract-drafting': 'Contract Drafting',
  'contract-lifecycle-management': 'Contract Lifecycle Management',
  'contract-negotiation': 'Contract Negotiation',
  'conversational-ai': 'Conversational AI',
  'copywriting': 'Copywriting',
  'cordova': 'Cordova',
  'couchdb': 'CouchDB',
  'coupa': 'Coupa',
  'cpp': 'C++',
  'cpq': 'CPQ',
  'creo': 'Creo',
  'crewai': 'CrewAI',
  'crm': 'CRM',
  'crowdstrike': 'Crowdstrike',
  'cryptocurrency': 'Cryptocurrency',
  'cryptography': 'Cryptography',
  'csharp': 'C#',
  'css': 'CSS',
  'cucumber': 'Cucumber',
  'cuda': 'CUDA',
  'customer-health-score': 'Customer Health Score',
  'customer-io': 'Customer.io',
  'customer-onboarding': 'Customer Onboarding',
  'customer-retention': 'Customer Retention',
  'cyberark': 'Cyberark',
  'cybersecurity': 'Cybersecurity',
  'cypress': 'Cypress',
  'dagster': 'Dagster',
  'dart': 'Dart',
  'dask': 'Dask',
  'dast': 'DAST',
  'data-analytics': 'Data Analytics',
  'data-engineering': 'Data Engineering',
  'data-governance': 'Data Governance',
  'data-ingestion': 'Data Ingestion',
  'data-lake': 'Data Lake',
  'data-lineage': 'Data Lineage',
  'data-mining': 'Data Mining',
  'data-modeling': 'Data Modeling',
  'data-pipelines': 'Data Pipelines',
  'data-quality': 'Data Quality',
  'data-science': 'Data Science',
  'data-visualization': 'Data Visualization',
  'data-warehousing': 'Data Warehousing',
  'databricks': 'Databricks',
  'datadog': 'Datadog',
  'dataverse': 'Dataverse',
  'davinci-resolve': 'Davinci Resolve',
  'db2': 'Db2',
  'dbt': 'dbt',
  'debezium': 'Debezium',
  'deep-learning': 'Deep Learning',
  'deepspeed': 'DeepSpeed',
  'defi': 'DeFi',
  'delta-lake': 'Delta Lake',
  'demand-generation': 'Demand Generation',
  'design-patterns': 'Design Patterns',
  'design-thinking': 'Design Thinking',
  'developer-advocacy': 'Developer Advocacy',
  'developer-experience': 'Developer Experience',
  'devops': 'DevOps',
  'devsecops': 'DevSecOps',
  'dfars': 'Dfars',
  'dhcp': 'DHCP',
  'dimensional-modeling': 'Dimensional Modeling',
  'distributed-computing': 'Distributed Computing',
  'distributed-systems': 'Distributed Systems',
  'distributed-training': 'Distributed Training',
  'django': 'Django',
  'dlp': 'DLP',
  'dns': 'DNS',
  'docker': 'Docker',
  'docs-as-code': 'Docs as Code',
  'dotnet': '.NET',
  'dropwizard': 'Dropwizard',
  'druid': 'Druid',
  'dynamodb': 'DynamoDB',
  'dynatrace': 'Dynatrace',
  'ec2': 'EC2',
  'eclipse': 'Eclipse',
  'ecommerce': 'E-commerce',
  'ecs': 'ECS',
  'edi': 'EDI',
  'edr': 'EDR',
  'eks': 'EKS',
  'elasticsearch': 'Elasticsearch',
  'elixir': 'Elixir',
  'elk': 'ELK',
  'elt': 'ELT',
  'email-marketing': 'Email Marketing',
  'embedded-linux': 'Embedded Linux',
  'embedded-software': 'Embedded Software',
  'embedded-systems': 'Embedded Systems',
  'embeddings': 'Embeddings',
  'ember': 'Ember.js',
  'employee-relations': 'Employee Relations',
  'employer-branding': 'Employer Branding',
  'entra-id': 'Entra ID',
  'envoy': 'Envoy',
  'erlang': 'Erlang',
  'erp': 'ERP',
  'ethereum': 'Ethereum',
  'ethernet': 'Ethernet',
  'ethersjs': 'ethers.js',
  'etl': 'ETL',
  'event-driven-architecture': 'Event Driven Architecture',
  'eventbridge': 'Eventbridge',
  'evm': 'EVM',
  'express': 'Express.js',
  'faiss': 'FAISS',
  'fastapi': 'FastAPI',
  'fastify': 'Fastify',
  'fea': 'Fea',
  'feature-engineering': 'Feature Engineering',
  'fedramp': 'Fedramp',
  'fhir': 'FHIR',
  'fiber': 'Fiber',
  'figjam': 'FigJam',
  'figma': 'Figma',
  'final-cut-pro': 'Final Cut Pro',
  'financial-modeling': 'Financial Modeling',
  'financial-reporting': 'Financial Reporting',
  'fine-tuning': 'Fine Tuning',
  'finops': 'FinOps',
  'fintech': 'Fintech',
  'firebase': 'Firebase',
  'firewall': 'Firewall',
  'firmware': 'Firmware',
  'fivetran': 'Fivetran',
  'flask': 'Flask',
  'flink': 'Flink',
  'flutter': 'Flutter',
  'flux': 'Flux',
  'fortinet': 'Fortinet',
  'fortran': 'Fortran',
  'fpga': 'FPGA',
  'fsharp': 'Fsharp',
  'full-cycle-recruiting': 'Full Cycle Recruiting',
  'fusion-360': 'Fusion 360',
  'gainsight': 'Gainsight',
  'game-development': 'Game Development',
  'gap-analysis': 'Gap Analysis',
  'gatling': 'Gatling',
  'gcp': 'GCP',
  'gd-t': 'Gd T',
  'gdpr': 'Gdpr',
  'gemini': 'Gemini',
  'general-ledger': 'General Ledger',
  'generative-ai': 'Generative AI',
  'generative-engine-optimization': 'Generative Engine Optimization',
  'gin': 'Gin',
  'gis': 'GIS',
  'git': 'Git',
  'github': 'GitHub',
  'github-actions': 'GitHub Actions',
  'github-copilot': 'Github Copilot',
  'gitlab': 'GitLab',
  'gitops': 'GitOps',
  'gke': 'GKE',
  'go': 'Go',
  'go-to-market': 'Go-to-Market',
  'godot': 'Godot',
  'gong': 'Gong',
  'google-ads': 'Google Ads',
  'google-analytics': 'Google Analytics',
  'google-search-console': 'Google Search Console',
  'google-tag-manager': 'Google Tag Manager',
  'gorilla': 'Gorilla',
  'gradle': 'Gradle',
  'grafana': 'Grafana',
  'grails': 'Grails',
  'graphql': 'GraphQL',
  'groovy': 'Groovy',
  'grpc': 'gRPC',
  'hadoop': 'Hadoop',
  'hapi': 'hapi',
  'haskell': 'Haskell',
  'hbase': 'HBase',
  'helm': 'Helm',
  'help-desk': 'Help Desk',
  'hibernate': 'Hibernate',
  'hipaa': 'Hipaa',
  'hive': 'Hive',
  'hl7': 'HL7',
  'hmi': 'Hmi',
  'hootsuite': 'Hootsuite',
  'houdini': 'Houdini',
  'hpc': 'HPC',
  'html': 'HTML',
  'hubspot': 'HubSpot',
  'huggingface': 'Hugging Face',
  'hyper-v': 'Hyper-V',
  'i2c': 'I2c',
  'iam': 'IAM',
  'iceberg': 'Iceberg',
  'iis': 'Iis',
  'illustrator': 'Illustrator',
  'image-processing': 'Image Processing',
  'indesign': 'InDesign',
  'influencer-marketing': 'Influencer Marketing',
  'influxdb': 'InfluxDB',
  'information-architecture': 'Information Architecture',
  'infrastructure-as-code': 'Infrastructure as Code',
  'interaction-design': 'Interaction Design',
  'intrusion-detection': 'Intrusion Detection',
  'intune': 'Intune',
  'invision': 'InVision',
  'ionic': 'Ionic',
  'ios': 'iOS',
  'ipsec': 'IPsec',
  'iso-13485': 'Iso 13485',
  'iso-26262': 'Iso 26262',
  'iso-27001': 'ISO 27001',
  'iso-9001': 'Iso 9001',
  'istio': 'Istio',
  'itar': 'Itar',
  'itil': 'ITIL',
  'j2ee': 'J2EE',
  'jamf': 'Jamf',
  'java': 'Java',
  'javascript': 'JavaScript',
  'jax': 'JAX',
  'jboss': 'Jboss',
  'jcl': 'Jcl',
  'jdbc': 'Jdbc',
  'jenkins': 'Jenkins',
  'jest': 'Jest',
  'jetpack': 'Jetpack',
  'jira': 'Jira',
  'jmeter': 'JMeter',
  'jms': 'Jms',
  'jpa': 'JPA',
  'jquery': 'jQuery',
  'json': 'JSON',
  'jsp': 'Jsp',
  'jtag': 'JTAG',
  'julia': 'Julia',
  'juniper': 'Juniper',
  'junit': 'JUnit',
  'jupyter': 'Jupyter',
  'jvm': 'JVM',
  'jwt': 'JWT',
  'k6': 'k6',
  'kafka': 'Kafka',
  'kanban': 'Kanban',
  'keras': 'Keras',
  'keycloak': 'Keycloak',
  'kibana': 'Kibana',
  'kicad': 'KiCad',
  'kinesis': 'Kinesis',
  'klaviyo': 'Klaviyo',
  'koa': 'Koa',
  'kotlin': 'Kotlin',
  'ktor': 'Ktor',
  'kubeflow': 'Kubeflow',
  'kubernetes': 'Kubernetes',
  'kvm': 'Kvm',
  'labview': 'LabVIEW',
  'lakehouse': 'Lakehouse',
  'lambda': 'Lambda',
  'langchain': 'LangChain',
  'langgraph': 'LangGraph',
  'langsmith': 'LangSmith',
  'laravel': 'Laravel',
  'ldap': 'LDAP',
  'lead-generation': 'Lead Generation',
  'legal-research': 'Legal Research',
  'lgpd': 'Lgpd',
  'lidar': 'LiDAR',
  'lifecycle-marketing': 'Lifecycle Marketing',
  'link-building': 'Link Building',
  'linkedin-ads': 'LinkedIn Ads',
  'linkedin-recruiter': 'LinkedIn Recruiter',
  'linkedin-sales-navigator': 'Linkedin Sales Navigator',
  'linux': 'Linux',
  'llamaindex': 'LlamaIndex',
  'llm': 'LLM',
  'llmops': 'LLMOps',
  'logstash': 'Logstash',
  'loki': 'Loki',
  'looker': 'Looker',
  'looker-studio': 'Looker Studio',
  'lottie': 'Lottie',
  'lua': 'Lua',
  'lwc': 'LWC',
  'machine-learning': 'Machine Learning',
  'macos': 'macOS',
  'madcap-flare': 'MadCap Flare',
  'mailchimp': 'Mailchimp',
  'mainframe': 'Mainframe',
  'mariadb': 'MariaDB',
  'marketing-automation': 'Marketing Automation',
  'marketo': 'Marketo',
  'master-data-management': 'Master Data Management',
  'matlab': 'MATLAB',
  'matplotlib': 'Matplotlib',
  'maven': 'Maven',
  'maximo': 'Maximo',
  'maya': 'Maya',
  'mcp': 'MCP',
  'mdm': 'MDM',
  'memcached': 'Memcached',
  'meta-ads': 'Meta Ads',
  'metabase': 'Metabase',
  'metadata-management': 'Metadata Management',
  'metasploit': 'Metasploit',
  'microcontrollers': 'Microcontrollers',
  'micronaut': 'Micronaut',
  'microservices': 'Microservices',
  'microsoft-access': 'Microsoft Access',
  'microsoft-copilot': 'Microsoft Copilot',
  'microsoft-dynamics': 'Microsoft Dynamics',
  'microsoft-fabric': 'Microsoft Fabric',
  'microstation': 'Microstation',
  'milvus': 'Milvus',
  'minitab': 'Minitab',
  'miro': 'Miro',
  'mistral': 'Mistral',
  'mitre-attack': 'MITRE ATT&CK',
  'mixpanel': 'Mixpanel',
  'mlflow': 'MLflow',
  'mlops': 'MLOps',
  'mobx': 'MobX',
  'mocha': 'Mocha',
  'mockito': 'Mockito',
  'modbus': 'Modbus',
  'model-deployment': 'Model Deployment',
  'model-evaluation': 'Model Evaluation',
  'monday-com': 'Monday.com',
  'mongodb': 'MongoDB',
  'motion-design': 'Motion Design',
  'motion-graphics': 'Motion Graphics',
  'mpls': 'MPLS',
  'mqtt': 'MQTT',
  'mulesoft': 'Mulesoft',
  'multithreading': 'Multithreading',
  'mvc': 'MVC',
  'mvvm': 'MVVM',
  'mysql': 'MySQL',
  'n8n': 'n8n',
  'nagios': 'Nagios',
  'nats': 'NATS',
  'navisworks': 'Navisworks',
  'neo4j': 'Neo4j',
  'nessus': 'Nessus',
  'nestjs': 'NestJS',
  'netsuite': 'NetSuite',
  'network-security': 'Network Security',
  'networking': 'Networking',
  'neural-networks': 'Neural Networks',
  'newrelic': 'New Relic',
  'nextjs': 'Next.js',
  'nginx': 'Nginx',
  'nifi': 'NiFi',
  'nim': 'Nim',
  'nist': 'Nist',
  'nlp': 'NLP',
  'nmap': 'Nmap',
  'nodejs': 'Node.js',
  'nosql': 'NoSQL',
  'notion': 'Notion',
  'npm': 'Npm',
  'numpy': 'NumPy',
  'nuxt': 'Nuxt',
  'oauth': 'OAuth',
  'objective-c': 'Objective-C',
  'observability': 'Observability',
  'ocaml': 'OCaml',
  'okta': 'Okta',
  'ollama': 'Ollama',
  'onedrive': 'Onedrive',
  'onnx': 'ONNX',
  'oop': 'OOP',
  'openai': 'OpenAI',
  'openapi': 'OpenAPI',
  'opencv': 'OpenCV',
  'openid': 'OpenID',
  'opensearch': 'OpenSearch',
  'openshift': 'OpenShift',
  'openstack': 'OpenStack',
  'opentelemetry': 'OpenTelemetry',
  'oracle': 'Oracle',
  'oscilloscope': 'Oscilloscope',
  'osint': 'Osint',
  'ospf': 'OSPF',
  'owasp': 'OWASP',
  'pagerduty': 'PagerDuty',
  'paid-social': 'Paid Social',
  'palo-alto-networks': 'Palo Alto Networks',
  'pandas': 'pandas',
  'parquet': 'Parquet',
  'pcb': 'Pcb',
  'pci-dss': 'Pci Dss',
  'pcie': 'PCIe',
  'peft': 'PEFT',
  'penetration-testing': 'Penetration Testing',
  'people-analytics': 'People Analytics',
  'peoplesoft': 'PeopleSoft',
  'performance-management': 'Performance Management',
  'perl': 'Perl',
  'pgvector': 'pgvector',
  'photoshop': 'Photoshop',
  'php': 'PHP',
  'pinecone': 'Pinecone',
  'pinot': 'Pinot',
  'pipeline-management': 'Pipeline Management',
  'pki': 'PKI',
  'playwright': 'Playwright',
  'plc': 'Plc',
  'plotly': 'Plotly',
  'plsql': 'PL/SQL',
  'pmp': 'PMP',
  'podman': 'Podman',
  'postgresql': 'PostgreSQL',
  'postman': 'Postman',
  'power-automate': 'Power Automate',
  'power-platform': 'Power Platform',
  'power-query': 'Power Query',
  'powerapps': 'Power Apps',
  'powerbi': 'Power BI',
  'powershell': 'PowerShell',
  'ppc': 'PPC',
  'pre-sales': 'Pre-sales',
  'preact': 'Preact',
  'predictive-analytics': 'Predictive Analytics',
  'predictive-modeling': 'Predictive Modeling',
  'prefect': 'Prefect',
  'premiere-pro': 'Premiere Pro',
  'presto': 'Presto',
  'primavera-p6': 'Primavera P6',
  'process-improvement': 'Process Improvement',
  'process-modeling': 'Process Modeling',
  'procore': 'Procore',
  'professional-scrum-master': 'Professional Scrum Master',
  'program-management': 'Program Management',
  'prometheus': 'Prometheus',
  'prompt-engineering': 'Prompt Engineering',
  'proof-of-concept': 'Proof of Concept',
  'protobuf': 'Protobuf',
  'protopie': 'ProtoPie',
  'prototyping': 'Prototyping',
  'pulsar': 'Pulsar',
  'pulumi': 'Pulumi',
  'puppet': 'Puppet',
  'puppeteer': 'Puppeteer',
  'pyspark': 'PySpark',
  'pytest': 'pytest',
  'python': 'Python',
  'pytorch': 'PyTorch',
  'qdrant': 'Qdrant',
  'qgis': 'QGIS',
  'qlik': 'Qlik',
  'qos': 'QoS',
  'quantization': 'Quantization',
  'quarkus': 'Quarkus',
  'quickbooks': 'QuickBooks',
  'r': 'R',
  'rabbitmq': 'RabbitMQ',
  'rag': 'RAG',
  'rails': 'Rails',
  'rancher': 'Rancher',
  'rbac': 'RBAC',
  'rdbms': 'RDBMS',
  'rds': 'RDS',
  'react': 'React',
  'react-native': 'React Native',
  'recommendation-systems': 'Recommendation Systems',
  'redis': 'Redis',
  'redshift': 'Redshift',
  'redux': 'Redux',
  'regulatory-compliance': 'Regulatory Compliance',
  'reinforcement-learning': 'Reinforcement Learning',
  'remix': 'Remix',
  'renewal-management': 'Renewal Management',
  'requirements-elicitation': 'Requirements Elicitation',
  'requirements-gathering': 'Requirements Gathering',
  'rest': 'REST',
  'revenue-recognition': 'Revenue Recognition',
  'reverse-engineering': 'Reverse Engineering',
  'revit': 'Revit',
  'rhel': 'RHEL',
  'rhino': 'Rhino',
  'rlhf': 'RLHF',
  'robot-framework': 'Robot Framework',
  'ros2': 'ROS 2',
  'rpa': 'RPA',
  'rspec': 'RSpec',
  'rtl': 'Rtl',
  'rtos': 'RTOS',
  'ruby': 'Ruby',
  'rust': 'Rust',
  'rxjs': 'RxJS',
  'saas': 'SaaS',
  'safe-agile': 'SAFe Agile',
  'sagemaker': 'SageMaker',
  'sales-cloud': 'Sales Cloud',
  'sales-enablement': 'Sales Enablement',
  'sales-engineering': 'Sales Engineering',
  'salesforce': 'Salesforce',
  'salesforce-marketing-cloud': 'Salesforce Marketing Cloud',
  'salesloft': 'Salesloft',
  'saml': 'SAML',
  'sap': 'SAP',
  'sap-hana': 'Sap Hana',
  'sap-mm': 'SAP MM',
  'sap-s4hana': 'SAP S/4HANA',
  'sas': 'SAS',
  'sase': 'Sase',
  'sass': 'Sass',
  'sast': 'SAST',
  'scada': 'SCADA',
  'scala': 'Scala',
  'sccm': 'SCCM',
  'scikit-learn': 'scikit-learn',
  'scim': 'SCIM',
  'scipy': 'SciPy',
  'screaming-frog': 'Screaming Frog',
  'scrum': 'Scrum',
  'scss': 'Scss',
  'scylladb': 'ScyllaDB',
  'sd-wan': 'SD-WAN',
  'sdlc': 'SDLC',
  'sdn': 'Sdn',
  'seaborn': 'Seaborn',
  'secrets-management': 'Secrets Management',
  'secure-coding': 'Secure Coding',
  'selenium': 'Selenium',
  'semantic-kernel': 'Semantic Kernel',
  'semantic-search': 'Semantic Search',
  'semrush': 'Semrush',
  'sensor-fusion': 'Sensor Fusion',
  'sentence-transformers': 'Sentence Transformers',
  'sentry': 'Sentry',
  'seo': 'SEO',
  'serverless': 'Serverless',
  'service-desk': 'Service Desk',
  'servicenow': 'ServiceNow',
  'sftp': 'SFTP',
  'sharepoint': 'SharePoint',
  'shopify': 'Shopify',
  'siem': 'SIEM',
  'siemens-nx': 'Siemens NX',
  'signal-processing': 'Signal Processing',
  'simulink': 'Simulink',
  'sinatra': 'Sinatra',
  'six-sigma': 'Six Sigma',
  'sketch': 'Sketch',
  'sketchup': 'SketchUp',
  'slack': 'Slack',
  'smart-contracts': 'Smart Contracts',
  'smartrecruiters': 'SmartRecruiters',
  'smartsheet': 'Smartsheet',
  'snmp': 'SNMP',
  'snowflake': 'Snowflake',
  'snowpark': 'Snowpark',
  'sns': 'SNS',
  'soa': 'SOA',
  'soapui': 'SoapUI',
  'soc-2': 'SOC 2',
  'solana': 'Solana',
  'solarwinds': 'Solarwinds',
  'soldering': 'Soldering',
  'solidity': 'Solidity',
  'solidjs': 'SolidJS',
  'solidworks': 'SolidWorks',
  'solr': 'Solr',
  'solution-design': 'Solution Design',
  'sonarqube': 'SonarQube',
  'soql': 'SOQL',
  'spacy': 'spaCy',
  'spark': 'Spark',
  'splunk': 'Splunk',
  'spring': 'Spring',
  'sprout-social': 'Sprout Social',
  'spss': 'SPSS',
  'sql': 'SQL',
  'sql-server': 'SQL Server',
  'sqlite': 'SQLite',
  'sqs': 'SQS',
  'ssh': 'SSH',
  'ssis': 'SSIS',
  'ssl': 'SSL',
  'sso': 'SSO',
  'ssrs': 'SSRS',
  'stakeholder-management': 'Stakeholder Management',
  'stata': 'Stata',
  'statistics': 'Statistics',
  'storyboarding': 'Storyboarding',
  'storybook': 'Storybook',
  'strategic-planning': 'Strategic Planning',
  'streamlit': 'Streamlit',
  'structured-authoring': 'Structured Authoring',
  'substance-designer': 'Substance Designer',
  'substance-painter': 'Substance Painter',
  'successfactors': 'SuccessFactors',
  'succession-planning': 'Succession Planning',
  'superset': 'Superset',
  'svelte': 'Svelte',
  'svn': 'SVN',
  'swagger': 'Swagger',
  'swift': 'Swift',
  'swiftui': 'SwiftUI',
  'symfony': 'Symfony',
  'sysml': 'SysML',
  'systemverilog': 'SystemVerilog',
  'tableau': 'Tableau',
  'tailwind': 'Tailwind',
  'talend': 'Talend',
  'talent-management': 'Talent Management',
  'talent-sourcing': 'Talent Sourcing',
  'tcl': 'Tcl',
  'tcp-ip': 'TCP/IP',
  'tdd': 'TDD',
  'teamcenter': 'Teamcenter',
  'teamcity': 'Teamcity',
  'technical-discovery': 'Technical Discovery',
  'technical-evangelism': 'Technical Evangelism',
  'technical-screening': 'Technical Screening',
  'technical-seo': 'Technical SEO',
  'technical-writing': 'Technical Writing',
  'tensorflow': 'TensorFlow',
  'tensorrt': 'TensorRT',
  'teradata': 'Teradata',
  'terraform': 'Terraform',
  'test-automation': 'Test Automation',
  'testify': 'Testify',
  'testng': 'TestNG',
  'testrail': 'Testrail',
  'threat-hunting': 'Threat Hunting',
  'threejs': 'Three.js',
  'thrift': 'Thrift',
  'ticket-resolution': 'Ticket Resolution',
  'tiktok-ads': 'TikTok Ads',
  'time-series': 'Time Series',
  'timescaledb': 'TimescaleDB',
  'tls': 'TLS',
  'tokenomics': 'Tokenomics',
  'tomcat': 'Tomcat',
  'transformers': 'Transformers',
  'travis': 'Travis CI',
  'trello': 'Trello',
  'trino': 'Trino',
  'triton': 'Triton',
  'typescript': 'TypeScript',
  'typography': 'Typography',
  'uart': 'UART',
  'ubuntu': 'Ubuntu',
  'udp': 'UDP',
  'uikit': 'Uikit',
  'uipath': 'UiPath',
  'uml': 'UML',
  'unit-testing': 'Unit Testing',
  'unity': 'Unity',
  'unix': 'Unix',
  'unreal-engine': 'Unreal Engine',
  'usability-testing': 'Usability Testing',
  'user-flows': 'User Flows',
  'user-research': 'User Research',
  'user-stories': 'User Stories',
  'uvm': 'Uvm',
  'vault': 'Vault',
  'vba': 'VBA',
  'vbnet': 'VB.NET',
  'vector-databases': 'Vector Databases',
  'vector-search': 'Vector Search',
  'veeva': 'Veeva',
  'vendor-management': 'Vendor Management',
  'verilog': 'Verilog',
  'version-control': 'Version Control',
  'vertex-ai': 'Vertex AI',
  'vhdl': 'VHDL',
  'video-editing': 'Video Editing',
  'virtualization': 'Virtualization',
  'visio': 'Visio',
  'visual-studio': 'Visual Studio',
  'visualforce': 'Visualforce',
  'vite': 'Vite',
  'vitest': 'Vitest',
  'vlan': 'VLAN',
  'vllm': 'vLLM',
  'vmware': 'VMware',
  'voip': 'VoIP',
  'vpc': 'VPC',
  'vpn': 'VPN',
  'vue': 'Vue',
  'vulnerability-scanning': 'Vulnerability Scanning',
  'vxlan': 'Vxlan',
  'waf': 'WAF',
  'wan': 'WAN',
  'wasm': 'WebAssembly',
  'wcag': 'WCAG',
  'weaviate': 'Weaviate',
  'web3': 'Web3',
  'webflow': 'Webflow',
  'webgl': 'WebGL',
  'webhooks': 'Webhooks',
  'weblogic': 'Weblogic',
  'webpack': 'Webpack',
  'websockets': 'Websockets',
  'wifi': 'Wi-Fi',
  'windchill': 'Windchill',
  'windows-server': 'Windows Server',
  'wireframing': 'Wireframing',
  'wireshark': 'Wireshark',
  'wordpress': 'WordPress',
  'workday': 'Workday',
  'workflow-orchestration': 'Workflow Orchestration',
  'wpf': 'Wpf',
  'xamarin': 'Xamarin',
  'xcode': 'Xcode',
  'xdr': 'XDR',
  'xero': 'Xero',
  'xgboost': 'XGBoost',
  'xml': 'XML',
  'xslt': 'Xslt',
  'yaml': 'YAML',
  'yardi': 'Yardi',
  'yolo': 'YOLO',
  'zabbix': 'Zabbix',
  'zapier': 'Zapier',
  'zbrush': 'Zbrush',
  'zendesk': 'Zendesk',
  'zeplin': 'Zeplin',
  'zero-trust': 'Zero Trust',
  'zeromq': 'ZeroMQ',
  'zig': 'Zig',
  'zoom': 'Zoom',
  'zoominfo': 'Zoominfo',
  'zustand': 'Zustand',
} as const;
export type SkillLabels = typeof SKILL_LABELS;
export const FACET_ALIASES = {
  'administration': ['administrative assistant', 'data entry', 'executive assistant', 'legal secretary', 'medical secretary', 'office assistant', 'office manager', 'receptionist', 'secretary', 'администратор', 'делопроизводитель', 'офис-менеджер', 'секретарь'],
  'ai_engineering': ['agent engineer', 'ai agent engineer', 'ai automation engineer', 'ai engineer', 'ai product engineer', 'ai research engineer', 'ai software engineer', 'ai-automation engineer', 'ai-product engineer', 'ai-research engineer', 'applied ai', 'genai', 'generative ai', 'llm', 'llm engineer', 'prompt engineer', 'rag engineer'],
  'architecture': ['architect', 'cloud architect', 'enterprise architect', 'software architect', 'solutions architect', 'архитектор'],
  'backend': ['1c', '1с', 'back end', 'back-end', 'back-end developer', 'backend', 'backend developer', 'бекенд', 'бэкенд'],
  'blockchain': ['blockchain', 'smart contract developer', 'web3 developer', 'блокчейн'],
  'business_analysis': ['business analysis', 'business analyst', 'business process analyst', 'business system analyst', 'business systems analyst', 'functional analyst', 'it business analyst', 'process analyst', 'requirements analyst', 'system analyst', 'systems analyst', 'аналитик бизнес-процессов', 'аналитик требований', 'бизнес аналитик', 'бизнес-аналитик', 'системный аналитик'],
  'c_level': ['ceo', 'chief', 'cpo', 'cto', 'head of', 'vice president', 'vp', 'директор', 'руководитель'],
  'creative': ['2d artist', '3d artist', 'animator', 'audio design engineer', 'audio designer', 'character artist', 'concept artist', 'environment artist', 'illustrator', 'motion graphics artist', 'photo editor', 'photographer', 'sound design engineer', 'sound designer', 'storyboard artist', 'technical artist', 'vfx artist', 'video editor', 'video producer', 'videographer'],
  'customer_success': ['client success', 'customer onboarding', 'customer success', 'implementation consultant', 'implementation engineer', 'implementation specialist', 'onboarding manager', 'onboarding specialist', 'renewal manager', 'renewals manager', 'менеджер по работе с клиентами', 'менеджер по успеху клиентов', 'специалист по адаптации клиентов'],
  'data_analytics': ['analyst', 'analytics engineer', 'bi analyst', 'bi developer', 'bi fejlesztő', 'bi riportfejlesztő', 'bi-аналитик', 'business intelligence analyst', 'business intelligence developer', 'business intelligence engineer', 'data analyst', 'data analytics', 'data аналитик', 'power bi developer', 'tableau developer', 'аналитик', 'аналитик bi', 'аналитик данных'],
  'data_engineering': ['adattárház fejlesztő', 'data engineer', 'data engineering', 'data governance', 'data platform', 'data steward', 'etl developer', 'дата-инженер', 'инженер данных'],
  'data_science': ['data scien', 'data science', 'data scientist', 'дата-сайентист'],
  'design': ['design', 'design engineer, product', 'design system engineer', 'design systems engineer', 'designer', 'experience design engineer', 'game design engineer', 'product design engineer', 'service design engineer', 'ui', 'ui design engineer', 'ui/ux design engineer', 'ux', 'ux design engineer', 'web design engineer', 'дизайн', 'дизайнер'],
  'developer_relations': ['developer advocate', 'developer community manager', 'developer evangelist', 'developer experience engineer', 'developer relations', 'devrel', 'technical evangelist', 'деврел', 'технический евангелист'],
  'devops': ['cloud design engineer', 'cloud engineer', 'cloud migration engineer', 'cloud operations engineer', 'data center engineer', 'data center technician', 'database administrator', 'database engineer', 'devops', 'infrastructure engineer', 'it administrator', 'it infrastructure', 'linux administrator', 'linux systems engineer', 'ml ops', 'mlops', 'network operations engineer', 'oracle dba', 'platform engineer', 'platform engineering', 'release engineer', 'sap basis administrator', 'servicenow administrator', 'sharepoint administrator', 'sysadmin', 'system administrator', 'systems administrator', 'windows administrator', 'администратор баз данных', 'девопс', 'системный администратор'],
  'education': ['basketball coach', 'chess instructor', 'fitness coach', 'lecturer', 'preschool teacher', 'professor', 'soccer coach', 'substitute teacher', 'swim instructor', 'teacher', 'tutor', 'воспитатель', 'воспитателя', 'методист', 'педагог', 'преподаватель'],
  'embedded': ['beágyazott szoftverfejlesztő', 'embedded', 'firmware', 'logiciel embarqué', 'встраиваемые', 'встраиваемых'],
  'engineering_design': ['architectural designer', 'bim coordinator', 'bim designer', 'bim modeler', 'bim specialist', 'cad designer', 'civil designer', 'design engineer', 'design technician', 'die designer', 'drafter', 'draftsman', 'draughtsman', 'electrical designer', 'hvac designer', 'mechanical designer', 'mold designer', 'piping designer', 'plumbing designer', 'revit designer', 'structural designer', 'tool designer', 'инженер-проектировщик', 'конструктор'],
  'finance': ['accountant', 'accounting', 'accounts payable', 'accounts receivable', 'bookkeeper', 'cfo', 'chief financial officer', 'finance analyst', 'finance controller', 'finance manager', 'financial analyst', 'financial controller', 'financial manager', 'fp&a', 'head of finance', 'payroll', 'tax accountant', 'treasury', 'бухгалтер', 'главбух', 'главный бухгалтер', 'казначей', 'финансовый аналитик', 'финансовый директор'],
  'frontend': ['angular developer', 'front end', 'front-end', 'front-end developer', 'frontend', 'frontend developer', 'react developer', 'react.js developer', 'reactjs developer', 'vue developer', 'vue.js developer', 'vuejs developer', 'фронт', 'фронтенд'],
  'fullstack': ['full stack', 'full-stack', 'fullstack', 'фуллстак', 'фулстек'],
  'hardware': ['analog design engineer', 'analogue design engineer', 'asic design', 'chip design', 'dft design engineer', 'digital design engineer', 'electronics engineer', 'fpga', 'hardware', 'ic design', 'memory design engineer', 'mixed signal design engineer', 'mixed-signal design engineer', 'pcb design', 'pcb designer', 'pcb layout', 'physical design engineer', 'rf design engineer', 'rfic design', 'rtl design engineer', 'semiconductor design', 'silicon design', 'soc design', 'standard cell design', 'vlsi design'],
  'healthcare': ['caregiver', 'cna', 'dental assistant', 'dental hygienist', 'home health aide', 'licensed practical nurse', 'lpn', 'medical assistant', 'medication technician', 'nurse', 'nurse practitioner', 'occupational therapist', 'optometrist', 'patient care technician', 'patient coordinator', 'pharmacy technician', 'phlebotomist', 'physical therapist', 'physician', 'registered nurse', 'rn', 'surgical technician', 'veterinarian', 'veterinary technician', 'ветеринарный врач', 'врач', 'медбрат', 'медсестра', 'санитар', 'фельдшер'],
  'hospitality': ['banquet server', 'barista', 'bartender', 'busser', 'chef', 'cook', 'dishwasher', 'host', 'host/hostess', 'hostess', 'kitchen assistant', 'line cook', 'prep cook', 'server'],
  'hr': ['chro', 'compensation and benefits', 'head of people', 'hr business partner', 'hr director', 'hr generalist', 'hr manager', 'hrbp', 'human resources', 'learning and development', 'people operations', 'people ops', 'people partner', 'директор по персоналу', 'менеджер по персоналу', 'специалист по персоналу', 'эйчар'],
  'industrial_engineering': ['application engineer', 'applications engineer', 'automation engineer', 'building engineer', 'commissioning engineer', 'control engineer', 'controls engineer', 'environmental engineer', 'facilities engineer', 'field engineer', 'field service engineer', 'geotechnical engineer', 'industrial engineer', 'instrumentation engineer', 'maintenance engineer', 'manufacturing engineer', 'planning engineer', 'plant engineer', 'process engineer', 'production engineer', 'project engineer', 'quality engineer', 'resident engineer', 'safety engineer', 'service engineer', 'site engineer', 'supplier quality engineer', 'validation engineer', 'инженер', 'технолог'],
  'intern': ['intern', 'internship', 'trainee', 'стажер', 'стажировка', 'стажёр'],
  'junior': ['jr', 'junior', 'джун', 'джуниор', 'младший'],
  'lead': ['lead', 'team lead', 'teamlead', 'ведущий', 'тимлид'],
  'legal': ['attorney', 'compliance analyst', 'compliance manager', 'compliance officer', 'contract administrator', 'contract manager', 'contracts administrator', 'contracts manager', 'corporate counsel', 'data privacy manager', 'data privacy officer', 'data protection officer', 'general counsel', 'lawyer', 'legal assistant', 'legal counsel', 'legal manager', 'paralegal', 'privacy officer', 'regulatory affairs', 'комплаенс', 'корпоративный юрист', 'юрисконсульт', 'юрист'],
  'logistics': ['cdl driver', 'commercial driver', 'courier', 'delivery driver', 'delivery specialist', 'dispatcher', 'driver', 'forklift operator', 'fulfillment associate', 'truck driver', 'truck unloader', 'warehouse', 'warehouse assistant', 'warehouse associate', 'warehouse operator', 'warehouse supervisor', 'водитель', 'грузчик', 'заведующий складом', 'кладовщик', 'курьер', 'сборщик заказов', 'экспедитор'],
  'management': ['engineering manager', 'manager', 'team manager', 'менеджер'],
  'marketing': ['aeo manager', 'aeo specialist', 'answer engine optimization', 'app store optimization', 'aso specialist', 'brand manager', 'community manager', 'content creator', 'content writer', 'conversion rate optimization', 'copywriter', 'demand generation', 'event coordinator', 'event manager', 'events coordinator', 'events manager', 'generative engine optimization', 'generative search optimization', 'geo manager', 'geo specialist', 'growth hacker', 'growth marketer', 'link building', 'marketing', 'media buyer', 'paid media', 'paid search', 'paid social', 'pr manager', 'pr specialist', 'public relations', 'search engine optimization', 'seo', 'smm', 'social media', 'бренд-менеджер', 'комьюнити-менеджер', 'контент-маркетолог', 'контент-менеджер', 'копирайтер', 'маркетинг', 'маркетолог', 'пиар-менеджер', 'пиар-специалист', 'таргетолог'],
  'middle': ['mid', 'mid level', 'mid-level', 'middle', 'мидл', 'средний'],
  'ml_ai': ['ai/ml', 'computer vision engineer', 'deep learning', 'machine learning', 'machine learning engineer', 'ml engineer', 'ml/ai', 'nlp engineer'],
  'mobile': ['android', 'ios', 'mobilalkalmazás fejlesztő', 'mobilalkalmazás-fejlesztő', 'mobile', 'react native developer', 'мобильная', 'мобильный', 'мобильных'],
  'network_engineering': ['network administrator', 'network design engineer', 'network engineer', 'network engineering', 'network specialist', 'network technician', 'сетевой администратор', 'сетевой инженер'],
  'operations': ['administrative assistant', 'biz ops', 'business operations', 'chief of staff', 'chief operating officer', 'coo', 'executive assistant', 'facilities coordinator', 'facilities manager', 'head of operations', 'office manager', 'operations analyst', 'operations coordinator', 'operations manager', 'operations specialist', 'ops manager', 'procurement', 'procurement manager', 'purchasing manager', 'ассистент руководителя', 'закупщик', 'операционный директор', 'операционный менеджер', 'офис-менеджер', 'помощник руководителя', 'специалист по закупкам'],
  'personal_services': ['aesthetician', 'armed guard', 'barber', 'custodian', 'esthetician', 'housekeeper', 'housekeeping', 'janitor', 'lifeguard', 'master stylist', 'security guard', 'stylist', 'охранник', 'парикмахер', 'сиделка', 'уборщик', 'уборщица'],
  'principal': ['principal'],
  'product': ['product manager', 'product owner', 'продакт', 'продукт-менеджер'],
  'project_management': ['agile coach', 'agile transformation lead', 'agile transformation manager', 'delivery manager', 'program coordinator', 'program manager', 'programme manager', 'project administrator', 'project coordinator', 'project manager', 'release train engineer', 'safe practitioner', 'scaled agile framework', 'scrum master', 'scrum-master', 'проджект', 'проект-менеджер', 'скрам мастер', 'скрам-мастер'],
  'qa': ['design engineer in test', 'qa', 'quality assurance', 'sdet', 'software design engineer in test', 'test automation', 'test engineer', 'tester', 'тестирование', 'тестировщик'],
  'recruiting': ['employer branding', 'it recruiter', 'recruiter', 'recruitment consultant', 'recruitment specialist', 'sourcer', 'talent acquisition', 'talent partner', 'talent sourcer', 'tech recruiter', 'technical recruiter', 'рекрутер', 'рекрутёр', 'специалист по подбору персонала'],
  'retail': ['bakery clerk', 'brand ambassador', 'cashier', 'deli clerk', 'grocery clerk', 'meat clerk', 'merchandiser', 'merchandising', 'produce clerk', 'product demonstrator', 'retail associate', 'retail service specialist', 'sales associate', 'stock associate', 'store driver', 'store leader', 'team member'],
  'sales': ['account executive', 'account manager', 'bdr', 'business development', 'go to market engineer', 'go-to-market engineer', 'gtm engineer', 'revenue operations', 'revops', 'sales', 'sales operations', 'sdr', 'продаж', 'продажам', 'продажи'],
  'security': ['appsec', 'blue team', 'ciso', 'cyber security', 'cyber systems engineer', 'cybersecurity', 'devsecops', 'grc', 'iam', 'identity and access management', 'incident response', 'infosec', 'it auditor', 'penetration tester', 'penetration testing', 'pentest', 'pentester', 'red team', 'red teamer', 'security', 'soc analyst', 'threat intel', 'threat intelligence', 'vulnerability analyst', 'vulnerability management', 'безопасности', 'безопасность', 'инженер по защите информации', 'кибербезопасность'],
  'senior': ['senior', 'sr', 'сеньор', 'синьор', 'старший'],
  'skilled_trades': ['alignment technicians', 'automotive mechanic', 'automotive mechanics', 'automotive technician', 'automotive technicians', 'carpenter', 'diesel technician', 'electrician', 'field service technician', 'hvac technician', 'installation technician', 'machinist', 'maintenance technician', 'mechanic', 'mechanics', 'millwright', 'plumber', 'service technician', 'service technicians', 'tire technicians', 'welder', 'маляр', 'механик', 'плотник', 'рабочий по благоустройству', 'рабочий по комплексному обслуживанию', 'сварщик', 'слесарь', 'электрик', 'электромеханик', 'электромонтер', 'электромонтёр', 'электросварщик'],
  'software_engineering': ['.net developer', '.net fejlesztő', 'abap developer', 'abap entwickler', 'abap fejlesztő', 'adatbázis fejlesztő', 'ai native engineer', 'ai-native engineer', 'alkalmazás fejlesztő', 'alkalmazásfejlesztő', 'app developer', 'application developer', 'c# developer', 'c# fejlesztő', 'c++ developer', 'database developer', 'desarrollador de software', 'desarrollador java', 'desarrolladora de software', 'desarrolladora java', 'desenvolvedor de software', 'desenvolvedor java', 'desenvolvedora de software', 'desenvolvedora java', 'deweloper oprogramowania', 'dotnet developer', 'drupal developer', 'dynamics 365 developer', 'développeur java', 'développeur salesforce', 'engenheira de software', 'engenheiro de software', 'entwickler software', 'erp developer', 'founding engineer', 'game developer', 'go developer', 'go engineer', 'golang developer', 'golang engineer', 'ingegnere del software', 'ingegnere software', 'ingeniera de software', 'ingeniero de software', 'ingénieur logiciel', 'integration engineer', 'inżynier oprogramowania', 'it engineer', 'it systems engineer', 'java developer', 'java entwickler', 'java fejlesztő', 'java fejlesztők', 'java-entwickler', 'javascript developer', 'magento developer', 'mainframe developer', 'member of technical staff', 'member of the technical staff', 'mes developer', 'node developer', 'node.js developer', 'node.js fejlesztő', 'nodejs developer', 'odoo fejlesztő', 'odoo-fejlesztő', 'oracle developer', 'pega fejlesztő', 'php developer', 'php fejlesztő', 'programista .net', 'programista python', 'python developer', 'python fejlesztő', 'rails developer', 'rpa developer', 'ruby developer', 'salesforce administrator', 'salesforce consultant', 'salesforce developer', 'salesforce engineer', 'sap developer', 'servicenow developer', 'servicenow engineer', 'sharepoint developer', 'shopify developer', 'software developer', 'software development engineer', 'software engineer', 'software entwickler', 'software mérnök', 'software systems engineer', 'software-entwickler', 'softwareentwickler', 'softwareingenieur', 'sviluppatore software', 'system engineer', 'systems engineer', 'szoftver fejlesztő', 'szoftverfejlesztő', 'szoftvermérnök', 'typescript developer', 'web developer', 'web engineer', 'webfejlesztő', 'wordpress developer', 'программист', 'разработчик'],
  'solutions_engineering': ['delivery consultant', 'deployment strategist', 'dynamics 365', 'dynamics 365 consultant', 'field application engineer', 'forward deployed engineer', 'forward-deployed engineer', 'hubspot crm administrator', 'integration consultant', 'microsoft dynamics', 'partner engineer', 'pre-sales consultant', 'pre-sales engineer', 'presales consultant', 'presales engineer', 'professional services consultant', 'professional services engineer', 'sales applications engineer', 'sales engineer', 'sap consultant', 'sap fico', 'sap mm consultant', 'sap sd consultant', 'solution consultant', 'solution design engineer', 'solution engineer', 'solutions consultant', 'solutions design engineer', 'solutions engineer', 'technical consultant', 'пресейл', 'пресейл-инженер'],
  'sre': ['site reliability', 'sre'],
  'staff': ['staff'],
  'support': ['call center', 'call-центр', 'contact center', 'customer care', 'customer service', 'help desk', 'helpdesk', 'it specialist', 'it technician', 'service desk', 'support', 'колл-центр', 'поддержка', 'поддержки', 'техподдержка', 'техподдержки'],
  'technical_writing': ['content designer', 'content strategist', 'curriculum designer', 'documentation engineer', 'documentation manager', 'documentation specialist', 'e-learning developer', 'elearning developer', 'information developer', 'instructional design', 'instructional designer', 'learning designer', 'learning experience designer', 'localization engineer', 'localization manager', 'localization specialist', 'technical communicator', 'technical writer', 'technical writing', 'translator', 'ux writer', 'ux-редактор', 'переводчик', 'разработчик документации', 'специалист по документации', 'технический писатель', 'технический редактор', 'техписатель'],
} as const;
export type FacetAliases = typeof FACET_ALIASES;
export const COLLECTIONS = [
  { slug: 'yc', title: 'Y Combinator', description: 'Open roles at Y Combinator–backed companies, from current batches to graduated unicorns.', kind: 'backer' },
  { slug: 'techstars', title: 'Techstars', description: 'Open roles at Techstars-backed companies.', kind: 'backer' },
  { slug: 'a16z-portfolio', title: 'a16z', description: 'Open roles at companies in the Andreessen Horowitz portfolio.', kind: 'backer' },
  { slug: 'a16z-speedrun', title: 'a16z Speedrun', description: 'Open roles at companies from the a16z Speedrun accelerator\'s cohorts.', kind: 'backer' },
  { slug: 'european', title: 'European Startups', description: 'Open roles at European startups across the continent\'s tech hubs.', kind: 'editorial' },
  { slug: 'ai', title: 'AI Companies', description: 'Open roles at AI-native companies — foundation-model labs, ML platforms and applied-AI products.', kind: 'editorial' },
  { slug: 'mag7', title: 'Magnificent Seven', description: 'Open roles at the Magnificent Seven — Apple, Microsoft, Alphabet, Amazon, Meta, Nvidia and Tesla.', kind: 'editorial' },
  { slug: 'bigtech', title: 'Big Tech', description: 'Open roles at the largest, most established technology companies.', kind: 'editorial' },
  { slug: 'unicorn', title: 'Unicorns', description: 'Open roles at unicorns — private companies valued at over $1 billion.', kind: 'editorial' },
  { slug: 'fortune500', title: 'Fortune 500', description: 'Open roles at Fortune 500 companies — the largest US corporations by revenue.', kind: 'editorial' },
  { slug: 'eastern-roots', title: 'Eastern Roots', description: 'Open roles at globally distributed companies founded by Eastern European (incl. Russian-speaking) founders or with Eastern European engineering roots.', kind: 'editorial' },
  { slug: 'indian-roots', title: 'Indian Roots', description: 'Open roles at companies founded in India or by Indian founders — the IT-services majors, the domestic product ecosystem, and the globally headquartered companies their founders built abroad.', kind: 'editorial' },
  { slug: 'ai-native', title: 'AI-Native', description: 'Open roles at AI-native companies building AI-first products and infrastructure — model and inference APIs, vector databases, and agent/dev tooling.', kind: 'editorial' },
  { slug: 'uk-skilled-worker-sponsor', title: 'Licensed UK sponsor', description: 'Open roles at employers on the GOV.UK register of licensed sponsors for the Skilled Worker and related work routes. The licence belongs to the employer — it is not a commitment to sponsor any particular role.', kind: 'credential' },
  { slug: 'nl-recognised-sponsor', title: 'Licensed NL sponsor', description: 'Open roles at employers on the IND public register of recognised sponsors for work and highly skilled migrants. The recognition belongs to the employer — it is not a commitment to sponsor any particular role.', kind: 'credential' },
  { slug: 'us-h1b-sponsor', title: 'H-1B sponsor history', description: 'Open roles at employers with USCIS-approved H-1B petitions in the last five fiscal years. Historical approval belongs to the employer — it is not a commitment to sponsor any particular role.', kind: 'credential' },
] as const;
export type Collection = (typeof COLLECTIONS)[number];
export type CollectionKind = Collection['kind'];
