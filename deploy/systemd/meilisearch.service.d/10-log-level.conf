# Meilisearch logs every HTTP request at INFO; on this host that was ~1.2 GB/day
# into /var/log/syslog. WARN keeps errors and startup, drops the request firehose.
[Service]
Environment=MEILI_LOG_LEVEL=WARN
