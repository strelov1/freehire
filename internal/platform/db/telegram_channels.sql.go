// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: telegram_channels.sql

package db

import (
	"context"
)

const listActiveTelegramChannels = `-- name: ListActiveTelegramChannels :many
SELECT channel, kind FROM telegram_channels
WHERE active
ORDER BY channel
`

type ListActiveTelegramChannelsRow struct {
	Channel string `json:"channel"`
	Kind    string `json:"kind"`
}

// The channels cmd/tg-ingest crawls and cmd/tg-extract reads a kind from. Ordered by
// name so a run's channel order is stable and its log diffable.
func (q *Queries) ListActiveTelegramChannels(ctx context.Context) ([]ListActiveTelegramChannelsRow, error) {
	rows, err := q.db.Query(ctx, listActiveTelegramChannels)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListActiveTelegramChannelsRow{}
	for rows.Next() {
		var i ListActiveTelegramChannelsRow
		if err := rows.Scan(&i.Channel, &i.Kind); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
