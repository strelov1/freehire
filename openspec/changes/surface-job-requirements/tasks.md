## 1. Render what is already stored

- [x] 1.1 Add the "What they ask for" section to `descriptionContent` in `web/src/lib/components/JobView.svelte`, between `<JobDescription>` and the Skills section: two groups in fixed order (`required`, then `preferred`), each entry's `text` verbatim, no section at all when the list is empty, `lang` taking `contentLang` like the description body. The grouping itself lives in `requirementGroups` in `web/src/lib/enrichment.ts`; the group headings come from `REQUIREMENT_PRIORITY_LABELS` in `web/src/lib/labels.ts`, which that file's own rule requires.
- [x] 1.2 Tests for the grouping in `web/src/lib/enrichment.test.ts`: both groups in fixed order; a single-priority list renders one group; an empty/absent list yields no groups; an entry duplicating a skill chip is kept; an unexpected priority is grouped the way `enrich.coerceRequirementPriority` coerces it; blank text is dropped.
  - **Deviation, deliberate:** three display-spec scenarios are NOT covered by a test — that the `{#if}` guard actually suppresses the section, that `lang` is set from `contentLang`, and that the text is interpolated (escaped) rather than `{@html}`. All three are properties of nine lines of markup with no branching in them. Covering them needs a Svelte compiler in the test run, and `web/vitest.config.ts` states in its own header that it runs plain Node with no Svelte compilation on purpose. Introducing component tests is a change to the repository's test architecture and belongs in its own change, not as a rider on this one.
- [x] 1.3 Run `pnpm --dir web lint`, `pnpm --dir web check` and `pnpm --dir web test` (the pre-commit hook runs eslint, CI additionally runs oxlint).

## 2. The deterministic extractor

- [x] 2.1 Create the extractor package `internal/job/reqextract` with the heading vocabulary and a `Derive(descriptionHTML string) []enrich.Requirement` entry point, and register it in the block table in `internal/platform/arch/layering/blocks.go` (a package in neither table fails the guard).
- [x] 2.2 Write the failing tests first, one per spec scenario: heading + list yields `required` entries in document order; an optionality heading yields `preferred`; both headings yield both; a benefits-only heading yields nothing; a matching heading with prose after it yields nothing; plain prose yields nothing.
- [x] 2.3 Implement the heading match and the list walk: match `<h1>`–`<h6>` and a `<strong>`/`<b>`/`<p>`/`<div>` short enough to be a title, take the list items of the first `<ul>`/`<ol>` after it, stop at the next heading, repeat for every matching heading.
- [x] 2.4 Implement text extraction: strip nested markup, decode entities, collapse whitespace, insert a space at block boundaries (markup is where the separation lives, so `Go<ul><li>generics` would otherwise read `Gogenerics`), drop an entry that is empty afterwards.
- [x] 2.5 Apply the bounds by calling `enrich.BoundRequirements` — exported from the unexported `boundRequirements` for exactly this — rather than restating `maxRequirements` / `maxRequirementTextRunes`; test that an over-long list and an over-long entry are clipped to whatever that function keeps.
- [x] 2.6 Run the extractor over real prod descriptions before trusting it. This found two false positives a unit test would not have imagined: `MUST HAVE MORNING/DAYTIME AVAILABILITY` heading a benefits list, and `Preferred Hours:` heading a scheduling note. Both are now regression cases in `reqextract_test.go`, and the fix is the heading-tail rule (a vocabulary phrase opens a section only when the rest of the heading is also vocabulary). Measured yield after the fix: 12.8% of 164 live postings.

## 3. Store the derivation

- [x] 3.1 Add migration `0135_jobs_requirements_derived.sql`: `jobs.requirements_derived jsonb NOT NULL DEFAULT '[]'::jsonb`, `IF NOT EXISTS` per squawk's `prefer-robust-stmts`.
- [x] 3.2 Thread the derived list through the write path — in `internal/job/job`'s `withDerived`, beside the two fingerprints, rather than in `internal/ingest/pipeline`: that is the one shared mapping every write path already goes through, so no path can omit it by forgetting a step.
- [x] 3.3 Add the column to `UpsertJob`, `UpsertManualJob`, `UpdateManualJob` and `InsertPrivateJob` (insert and conflict branches) and run `make sqlc`. All four, not just the crawl: a moderator-authored posting or a pasted private JD that silently never showed requirements is the "backend can do it, the UI cannot reach it" trap. The crawl and moderator re-create paths keep the stored list when the incoming derivation is empty (a failed detail fetch must not wipe one); the moderator EDIT path writes unconditionally, because deleting a list from a posting means it no longer has one.
- [x] 3.4 Tests: `internal/job/job` covers that the shared mapping derives a posting's list and stores `[]` (never null) when it yields none; `internal/platform/db` integration covers the column default and the keep-the-stored-value guard. Extended `mutateField` in `internal/job/jobhash`'s reflective coverage guard for the new `[]byte` field — the guard fails on a field it cannot mutate, so an unchecked column cannot read as checked.

## 4. Merge into the served field

- [x] 4.1 Add the third overlay to `SetJobEnrichment`, chained after the two salary overlays: when the incoming payload's `requirements` is empty and `requirements_derived` is not, overlay the derived list. Run `make sqlc`.
- [x] 4.2 The moderator re-create path needs no overlay of its own — unlike `SetJobEnrichment` it never replaces the enrichment blob, it only merges the manual salary onto the stored one, so a derived list already in `enrichment.requirements` survives it untouched.
- [x] 4.3 Integration tests for the overlay (`internal/platform/db/requirements_derived_integration_test.go`): a payload with requirements is left alone; a payload without them picks up the derivation; an empty array counts as stating none; neither source yields an absent field; a second enrichment run cannot erase the derived list; the derivation leaves the provenance stamp unenriched.
- [x] 4.4 The provenance half of 4.4 is covered by the integration test above. The `GET /api/v1/jobs/:id` half needs no new test: `jobview` passes `enrichment.requirements` through unchanged and a test already locks that, so once the overlay puts the list in the column the endpoint serves it by the same path the model's list takes.

## 5. Backfill the existing catalogue

- [x] 5.1 Add the three backfill queries to `internal/platform/db/queries/jobs.sql` and run `make sqlc`: the id bounds, the chunk read (open postings only, and deliberately no predicate on `description` — that de-TOASTs the column for every row examined), and the `unnest`-batched, `IS DISTINCT FROM`-guarded chunk write.
- [x] 5.2 Write `cmd/backfill-requirements` on the `internal/platform/worker` Main/Bootstrap plumbing: keyset over `id`, open postings only, `BACKFILL_REQUIREMENTS_CHUNK` (5k) for the id span, `BACKFILL_REQUIREMENTS_MAX` (200k) to cap one run, `BACKFILL_REQUIREMENTS_FROM_ID` to resume, `DATABASE_URL` the only requirement. Add it to `.gitignore` — `TestEveryCmdBinaryIsGitignored` fails otherwise, and a committed worker binary breaks `git pull` on prod.
- [x] 5.3 Tests: a unit test for `derive` (only rows yielding a list are written; the payload decodes as a requirements list; an empty chunk writes nothing) and integration tests for the two queries (the chunk read skips closed postings and honours its id range; the batched write fills a row and a re-run writes nothing).

## 6. Documentation and checks

- [x] 6.1 Add `backfill-requirements` to the worker gotchas list in `AGENTS.md` (`CLAUDE.md` is a symlink to it), stating that it is one-off, idempotent, open-postings-only, needs only `DATABASE_URL`, needs **no** reindex, and why it is not folded into `backfill-derive`.
- [x] 6.2 Note the two producers of `enrichment.requirements`, the fill-if-absent precedence, and the single shared `enrich.BoundRequirements` ceiling in `internal/ai/enrich/AGENTS.md` and in the extractor package's own doc comment. Register `reqextract` in `internal/job/AGENTS.md`'s package list too.
- [x] 6.3 Run the full gate: `gofmt -l .` prints nothing, `go vet ./...`, `go test ./...`, `go vet -tags=integration ./...`, `go test -tags=integration ./internal/platform/db/`, `golangci-lint run` (no new findings), `pnpm check:links` (290 links resolve), `pnpm --dir web lint`, `pnpm --dir web check` (0 errors), `pnpm --dir web test` (1458 pass).
