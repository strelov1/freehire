## 1. Backend: seniorities column and validation

- [x] 1.1 Add migration `migrations/0123_user_profile_seniorities.sql` adding `user_profiles.seniorities text[] NOT NULL DEFAULT '{}'`
- [x] 1.2 Update `internal/platform/db/queries/user_profiles.sql` (`GetUserProfile`, `UpsertUserProfile`, `UpsertUserProfileIfUnchanged`) to read/write `seniorities`; run `make sqlc`
- [x] 1.3 Add `Seniorities []string` to `userprofile.Profile`, a `normalizeSeniorities` (dedup preserving order, validate against `vocab.SeniorityValues`, no cap) mirroring the existing specialization validation, and a matching `ErrInvalidSeniority`; thread `seniorities` through `Service.Save`
- [x] 1.4 Update `internal/api/handler/me_profile.go`: `saveProfileRequest`/`profileResponse` gain `seniorities` (optional, defaults to empty); map `ErrInvalidSeniority` to `400`

## 2. Backend: tests

- [x] 2.1 Unit tests in `internal/identity/userprofile/userprofile_test.go`: unknown seniority rejected, dedup preserving order, empty seniorities accepted, multiple valid seniorities stored
- [x] 2.2 Handler tests in `internal/api/handler/me_profile_test.go`: `seniorities` round-trips through save/read, invalid value → `400`, omitted `seniorities` defaults to empty array
- [x] 2.3 `gofmt -w` the touched Go files, `go vet ./...`, `go test ./...`; `go vet -tags=integration ./...` before push

## 3. Frontend: seniorities plumbing

- [x] 3.1 Add `seniorities: string[]` to `UserProfile` and the save-profile request type in `web/src/lib/types.ts`
- [x] 3.2 Thread `seniorities` through `getProfile`/`saveProfile` in `web/src/lib/api.ts` and through `profileStore` (`web/src/lib/profile.svelte.ts`)

## 4. Frontend: the onboarding overlay component

- [x] 4.1 Create `web/src/lib/components/onboarding/PostRegistrationOnboarding.svelte`: local staged state (`specializations`, `skills`, `seniorities`, `location`), step counter (1-3), reusing the pill-group and CV-auto-fill patterns from `OnboardingWizard.svelte`
- [x] 4.2 Step 1 (CV): file upload via `api.extractResumeProfile`, merges extracted `categories`/`seniority`/`skills` into staged state, stays on step 1 for review (matches `OnboardingWizard.svelte`'s existing behavior), "Skip" advances without upload
- [x] 4.3 Step 2 (Confirm): three independent multi-select pill groups — Role (`CATEGORY_OPTIONS`), Skills (reuse the skills picker pattern), Level (`vocab`'s seniority facet options) — pre-filled from staged state and, on first open, from `profileStore.profile` if one exists
- [x] 4.4 Step 3 (Location): reuse the location picker pattern from `ProfileForm.svelte`, pre-filled from `profileStore.profile?.location_preferences`
- [x] 4.5 On close (finish, skip-to-end, or explicit dismiss): if `specializations` AND `skills` are both non-empty, call `profileStore.save(...)` once with the staged values; otherwise send nothing (caught by local verification: an `||`-vs-`&&` guard bug let a role-only save through to a 400 from the save endpoint — fixed)
- [x] 4.6 Gate condition: mount effect calls `resumeStore.ensureLoaded()`; component renders only when `isAuthenticated() && resumeStore.loaded && resumeStore.meta?.present === false`

## 5. Frontend: wire the gate into the app shell

- [x] 5.1 Render `<PostRegistrationOnboarding />` from `web/src/routes/+layout.svelte` alongside the existing self-gating overlays (near `CvRefreshDialog`)

## 6. Frontend: tests

- [x] 6.1 No Svelte component-render tests exist anywhere in this repo (confirmed: `OnboardingWizard.svelte` itself has none; the only `.svelte.test.ts` covers a plain store, not a component) — following that convention, no component test was added. Behavior verified via `svelte-check` (0 errors) and local manual verification (section 7).
- [x] 6.2 `pnpm run lint` (0 errors, only pre-existing repo-wide warnings unrelated to the touched files) and `pnpm run check` (0 errors)

## 7. Local verification

- [x] 7.1 Ran the real server (`go run ./cmd/server`) + a worktree-local Postgres + the SvelteKit dev server against it, driven with Playwright: registered a fresh account, confirmed the full-screen overlay appears, walked all 3 steps (skip-through and picked-values paths). CV upload itself (the LLM-backed extraction call) was not exercised — this dev environment has no `LLM_API_KEY` configured — but the upload button, the Skip path, and the confirm/location steps it feeds were all verified live.
- [x] 7.2 Confirmed the overlay reappears (still no CV) after closing without a CV, correctly pre-filled from a profile saved on a prior "session" (page reload) once the pre-fill race (see below) was fixed. CV-present suppression (`resumeStore.present`) is exercised by the store's own logic and the gate's `$derived` condition; not re-verified live end-to-end since real CV upload needs LLM credentials this dev env lacks.

**Two real bugs found and fixed by this live run** (not caught by `svelte-check`/lint/unit tests, since neither the guard logic nor the load-order race has a test harness in this repo for Svelte components):
- `finish()`'s skip guard used `&&` (skip only if BOTH specializations and skills are empty) instead of `||` — a role-only selection (e.g. the skills dictionary offering nothing, as it does against an empty catalogue) fired a `PUT` that the save endpoint correctly rejected with `400`, right after the gate had already closed. Fixed to `||`; spec/design updated to match and a new scenario added.
- The confirm/location pre-fill effect kept from `profileStore.profile` at the moment the gate became visible, but visibility depends only on `resumeStore` (CV presence) — `profileStore`'s own load frequently resolves a moment later, so the reseed sometimes ran against a still-`null` profile and never re-ran. Fixed to wait for `profileStore.loaded` and re-run when it (or the profile) changes, latching only once seeded per opening.

## 8. Post-review scope expansion (requested live, not in the original tasks above)

See design.md's "Post-implementation scope expansion" section for the full rationale on each.

- [x] 8.1 Converted the gate from an overlay in `+layout.svelte` to a dedicated `/onboarding` route, with a redirect-effect pair in the layout (to-onboarding: reactive; away-from-onboarding: `untrack()`-guarded, checked only at arrival) — fixing a real bug where the CV step's own upload flipped `resumeStore.present` and immediately bounced the user off the page mid-flow
- [x] 8.2 Role picker on the confirm step changed from a pill grid to a `RemoteSearchSelect` (search-as-you-type over `CATEGORY_OPTIONS`)
- [x] 8.3 Added a per-field "Clear" control (Role/Level/Skills), matching `FacetSection.svelte`'s pattern
- [x] 8.4 Added a conditional `auth` step (register, with an inline "Sign in instead" toggle) shown only while signed out; step list derives from `isAuthenticated()` so a successful auth mid-page carries straight into the CV step with no navigation
- [x] 8.5 Added `?returnTo=` threading (`onboardingUrl()` in `onboardingGate.svelte.ts`, validated with the existing `safeRedirect`) — every entry point to `/onboarding` carries it, and every way of leaving (auth-step skip/close, `finish()`) navigates there instead of a hardcoded `/`
- [x] 8.6 Retired `AuthDialog.svelte`'s register mode — "No account? Create one" now navigates to `/onboarding` — and `JobView.svelte`'s signed-out "Sign up" gate does the same
- [x] 8.7 Retired the anonymous `/jobs` `OnboardingWizard.svelte` — `JobsView.svelte`'s banner now navigates to `/onboarding` instead of opening it; the component is deleted (its pure-logic module `onboarding.ts` is left mostly intact — see design.md for what's dead-but-untouched and why)
- [x] 8.8 Two-column auth-step visual design (dark brand panel + credential form), at the user's request, modeled on a reference screenshot but restyled with this app's own tokens/copy, no fabricated content
- [x] 8.9 `go build/vet`, `pnpm run check`, `pnpm run lint`, `pnpm test` all re-verified green after every change in this section; live Playwright verification covered: anonymous visit → auth-step two-panel render → Skip → returnTo; register → same-page carry-through into the CV step; `AuthDialog`'s "Create one" → `/onboarding?returnTo=...`. The `/jobs` banner's own navigation was verified by code inspection only (this dev environment has no Meilisearch running, so the standalone jobs page 500s and the banner never renders to click)
- [x] 8.10 Specialization picker on the confirm step (further live feedback, reference screenshot of the job filter's `CategoryPane`): rebuilt locally as search + collapsible `CATEGORY_GROUPS` sections + pills, matching `CategoryPane.svelte`'s markup — not the component itself, which is wired to a `FacetStore` this page has no use for (same reason `RoleCard.svelte` doesn't reuse it either). Counts come from `api.facetCounts(..., { facets: ['category'] })`, best-effort (a failed/unavailable fetch just omits counts, same tolerance `CategoryPane` already has for a null `counts` prop) — not visible in this dev environment (no Meilisearch running) but verified structurally: search filtering, group collapse/expand, and pill selection all confirmed live via Playwright. Field relabeled Role → Specialization to match.
- [x] 8.11 Skills split out of the confirm step into its own step (`stepKinds` gained a `skills` entry between `confirm` and `location` — 4 steps signed in, 5 anonymous). Verified live: confirm step no longer shows a skills control, Skills renders on its own page with its own heading, step count reflects the addition (`Step 3 of 4`), Continue/Back/Skip all still work across the new boundary.
- [x] 8.12 `/simplify` pass: extracted the OAuth-provider label map + fetch (`api.oauthProviders()` + filter-by-known-label) that had been duplicated near-identically between `AuthDialog.svelte` and `/onboarding`'s auth step into `$lib/oauthProviders.ts` (`PROVIDER_LABELS`, `loadOAuthProviders()`); extracted the twice-repeated brand-mark-link markup on the auth step into a `brandLink` snippet. Backend diff (userprofile/repository/handler) reviewed — already minimal and consistent with existing patterns, nothing to simplify. Re-verified green: `go build/vet`, backend tests, `pnpm run check`, `pnpm run lint`, `pnpm test`.
- [x] 8.13 `code-review high` on the full diff (3 parallel finder angles + a 2-pass verification): fixed all 4 findings that survived verification plus several raw findings independently confirmed:
  - **`+layout.svelte`'s away-from-/onboarding redirect** read `resumeStore.loaded` inside `untrack()`, so a signed-in visitor with a CV who landed directly on `/onboarding` (bookmark/stale link) got stuck seeing the whole wizard forever — the effect's one run fired before the async CV-status fetch resolved, and nothing re-triggered it. Fixed: `resumeStore.loaded` moved outside `untrack()` (tracked), `isAuthenticated()`/`resumeStore.present` stay inside it (so the page's own mid-visit CV upload still doesn't yank the user off it — the earlier bug this same `untrack()` pattern was built to prevent).
  - **`onboardingGate.dismissedThisVisit`** was a bare `$state` flag never cleared on sign-out (not a `UserResource`, not swept by `resetUserStores()`), so account B signing in after account A dismissed onboarding on the same tab silently skipped the gate. Fixed: added `onboardingGate.reset()`, called alongside `resetUserStores()` in the layout's existing sign-out effect.
  - **`finish()` silently discarded staged Level/Location** whenever Specialization or Skills was empty (the save endpoint requires both non-empty for a profile to exist at all) — a user could pick only Level/Location, hit Finish, and lose those picks with zero feedback. Fixed: a `willLoseOrphanedPicks` heads-up now shows on the Skills/Location steps when this applies, rather than staying silent; `finish()`'s behavior (skip the save) is otherwise unchanged — everything stays skippable, nothing blocks.
  - **`onboarding.ts` dead code** (`OnboardingSelection`/`selectionsToQuery`/`emptySelection`, orphaned by this diff's own deletion of `OnboardingWizard.svelte`) — deleted, per CLAUDE.md's "clean up what your change orphaned". `markDone()` also deleted (zero callers), but the `'done'` lifecycle *value* was kept (a browser that stored it before this change must still read it correctly). `onboarding.test.ts` rewritten to match: `narrowestFacet` now builds `JobFilters` fixtures directly instead of round-tripping through the deleted `selectionsToQuery`.
  - **Auth step accessibility regression**: the deleted `OnboardingWizard.svelte` had `role="dialog"`/`aria-modal`/`focusTrap`; the new auth-step panel had none of the three (a keyboard/screen-reader user could tab out of the full-screen credential form onto the hidden page behind it). Fixed: added all three to the auth step's outer container, matching the other steps' shell.
  - **`pickCv()` lost its auth guard**: reachable in the rare case a session expires mid-visit while `step` still points at the CV step (stepKinds recomputes and 'cv' can land at the same index unauthenticated). Fixed: bounces to step 1 (now correctly the auth step) instead of opening a picker that would just 401.
  - **Duplicated 401/400 credential-error copy** between `AuthDialog.svelte` and the auth step — extracted to `$lib/credentialErrorMessage.ts`; also removed `AuthDialog`'s now-dead 409 branch (that error only comes from registration, which no longer happens in that dialog).
  - **`resumeStore.ensureLoaded()`** fired unconditionally in the layout for every visitor including anonymous ones (a guaranteed 401) — gated on `isAuthenticated()`.
  - Deferred, not fixed (flagged for a future change, not silently dropped): CV-upload-logic duplication between `/onboarding` and `ProfileForm.svelte` (the two call sites diverge enough — setup-mode vs. editing-mode branching — that a shared helper needs its own design pass); `facetCounts`/`loadSkillDistribution` fetching unconditionally at mount rather than gated per-step (minor wasted work on early abandonment, not a correctness issue); the onboarding gate's two `$effect`s living in the already-large root layout rather than a dedicated module (organization suggestion, not a bug — no "use*" composable precedent exists elsewhere in this codebase to follow); the old wizard's "peak intent" Telegram-alert offer has no equivalent in the new flow (the new flow's payoff is a saved profile, not a filter query — there is nothing analogous to alert on).
  - Re-verified green after every fix: `go build/vet`, backend tests, `pnpm run check`, `pnpm run lint`, `pnpm test` (1312 — down from 1319 as `onboarding.test.ts`'s dead-code tests were removed, not from any behavior loss), plus a full live Playwright pass covering the dialog-role fix, the register→CV-step carry-through, and the orphaned-picks warning appearing exactly when expected (present with Level-only + no specialization/skills, absent on the Confirm step itself, and the level-only Finish correctly sends no `PUT`).
